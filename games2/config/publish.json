{
  "$comment": [
    "THE PUBLICATION POLICY — what is IN THE GAME (maintainer 2026-08-14).",
    "",
    "This file is the ONLY hand-maintained part of the split. Everything else is",
    "derived by scripts/shipset.mjs, which closes over these roots: a published",
    "world drags in its own tiles, its NPCs' character art and its spawn zones'",
    "monsters automatically. Add an NPC to a shipped world and its art ships with",
    "no edit here.",
    "",
    "Anything NOT reachable from these roots is STAGING content: it stays in git,",
    "stays reviewable in the wiki for a signed-in admin (fetched from GitHub), and",
    "never enters the deployed image.",
    "",
    "Editing this file changes what production serves. The shipset digest changes",
    "with it, which is what makes the image and the tile atlas rebuild."
  ],

  "userWorlds": ["the_island2"],
  "$comment_userWorlds": [
    "What an END USER may select. Everything else in `worlds` below is a dev",
    "map: shipped so it WORKS, but hidden from the picker unless you are signed",
    "in as admin (build-worlds marks them dev:true, loadWorldsList filters them).",
    "",
    "This is a product gate, not a security boundary — the repo is public, so",
    "the maps are readable on GitHub by anyone who looks. The point is that the",
    "game never OFFERS them, which is what 'always prepared for the final",
    "release' actually needs."
  ],

  "worlds": [
    "the_island2",
    "the_island",
    "demo_isle",
    "demo_lost",
    "glow_test",
    "house_demo",
    "monster_demo",
    "occlusion_test",
    "prop_demo",
    "ring_test",
    "trans_demo"
  ],
  "$comment_worlds": [
    "Every world that ships. The dev maps are here because THE SERVER IS",
    "AUTHORITATIVE: WorldRoom.loadWorldGrid reads world.json and spawns.json",
    "from disk, so a map the image lacks cannot be joined at all — browser CORS",
    "cannot fix that, only a server-side GitHub fetch could.",
    "",
    "Cost of keeping them: 87 MB of art instead of 57 MB. Still 126 MB less than",
    "before the split, because the real win was never the worlds — it was the",
    "unused tiles, scenery and monsters the closure drops either way.",
    "",
    "To go leaner later, teach the server to fetch an unknown world from GitHub",
    "and pull the dev maps out of this list; the userWorlds gate above stays."
  ],

  "playableCharacters": ["humans/default_boy", "humans/default_girl"],

  "monsters": [],
  "$comment_monsters": [
    "Monsters reachable from a published world's spawns.json are found",
    "automatically. This list is only for monsters the ENGINE names directly",
    "with no spawn zone — leave empty unless something 404s."
  ],

  "scenery": ["campfire", "grave_cross", "blood_spatter"],
  "$comment_scenery": [
    "The three pieces WorldScene loads by hardcoded path. The v2 factory's other",
    "~2,640 pieces are staging content until the maps2 agent places them."
  ],

  "entityDomains": ["characters2", "monsters", "tiles2", "scenery"],
  "$comment_entityDomains": [
    "Domains where individual entities are selected by closure. Their ROOT-level",
    "files and config/ trees ship wholesale regardless — those are descriptors the",
    "manifest builders read (monsters/config/roster.json,",
    "characters2/animation_map.json, tiles2/emission.json). Omitting one does not",
    "404 a sprite, it silently empties a manifest: the first curated root built a",
    "game with ZERO monsters for exactly this reason."
  ],

  "alwaysShip": ["items", "lore", "live", "wiki", "sounds", "music"],
  "$comment_alwaysShip": [
    "Domains where per-entity closure buys little: items and lore are small and",
    "wholly in-game, live/ is runtime tuning state, wiki/ is the wiki itself.",
    "sounds/ and music/ are catalog-driven at runtime rather than referenced by",
    "the worlds, so they ship whole minus the excludes below — tightening those",
    "two to a real catalog closure is a follow-up, not a guess to make here."
  ],

  "exclude": [
    "^tiles2/[^/]+/raw/",
    "^music/.*\\.wav$",
    "^characters2/_experiments/",
    "^wiki/tools/",
    "^maps2/(pipeline|spec)/",
    "^[^/]+/pipeline/",
    "^[^/]+/README\\.md$"
  ],
  "$comment_exclude": [
    "Applied last, so these carve out of the wholesale alwaysShip includes.",
    "tiles2/*/raw and the music masters were already excluded by .dockerignore —",
    "kept here so ONE file is the source of truth for what ships."
  ]
}
