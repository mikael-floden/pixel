{
  "_comment": "Per-clip animation briefs for redo_facing_anim.py, keyed <group>/<piece>#<STATE>#<anim>. A redo that names the WRONG subject fails on the outline rule no matter how it is worded: the 2026-09-15 round told a barrel of cyan water, a skull's eye-light, a bush's leaves and a cairn's ripple to 'flicker the flame' (0.16-0.81 movement). Each brief here names the piece's own moving thing — the subject its original animate_motion brief named — in the wording that measured 0.0 on hearth_001 LIT_3: name no material that must hold still, say everything else is copied from the first frame pixel for pixel. Without an entry the tool falls back to STILL_PROMPT.",
  "barrels/barrel_007#LIT_2#motion": "This is the same picture in every frame. ONLY the outermost edge of the cyan water in the barrel changes: a few pixels along that edge shift very slightly in brightness and shift straight back. The water is lit and clearly visible in every frame, including the first. It stays exactly the same size and shape in every frame: it never grows and never spreads, only its inner detail shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "beast_skulls/beast_skull_001#LIT_1#motion": "This is the same picture in every frame. ONLY a small point of light inside the eye sockets moves, drifting slowly from one side to the other. It is lit and clearly visible in every frame, including the first. It stays exactly the same size in every frame: it never grows and never spreads. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "beds/bed_001#LIT_1#flame": "This is the same picture in every frame. ONLY the small candle flame flickers, very gently. It is lit and clearly visible in every frame, including the first. It stays exactly the same size and shape in every frame: it never grows and never spreads, only its inner detail shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "beds/bed_002#LIT_4#flame": "This is the same picture in every frame. ONLY the small candle flame flickers, very gently. It is lit and clearly visible in every frame, including the first. It stays exactly the same size and shape in every frame: it never grows and never spreads, only its inner detail shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "braziers/brazier_014#LIT_3#flame": "This is the same picture in every frame. ONLY the fire in the bowl flickers. It is lit and clearly visible in every frame, including the first. It stays exactly the same size and shape in every frame: it never grows and never spreads, only its inner detail shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "braziers/brazier_018#LIT_4#flame": "This is the same picture in every frame. ONLY the fire in the bowl flickers. It is lit and clearly visible in every frame, including the first. It stays exactly the same size and shape in every frame: it never grows and never spreads, only its inner detail shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "briar_thickets/briar_thicket_013#LIT_1#motion": "This is the same picture in every frame. ONLY the glowing green arcs brighten a little and return. They are lit and clearly visible in every frame, including the first. They stay exactly the same size and shape in every frame: they never grow and never spread, only their brightness shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "briar_thickets/briar_thicket_016#LIT_4#flame": "This is the same picture in every frame. ONLY the flame flickers. It is lit and clearly visible in every frame, including the first. It stays exactly the same size and shape in every frame: it never grows and never spreads, only its inner detail shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "briar_thickets/briar_thicket_021#LIT_1#motion": "This is the same picture in every frame. ONLY the glowing green lines brighten a little and return. They are lit and clearly visible in every frame, including the first. They stay exactly the same size and shape in every frame: they never grow and never spread, only their brightness shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "bushes/bush_001#NOT_LIT_1#motion": "This is the same picture in every frame. ONLY the leaves move, shivering very slightly in a faint breeze: no leaf shifts by more than a pixel or two, and the bush keeps exactly its outline. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "bushes/bush_007#LIT_2#motion": "This is the same picture in every frame. ONLY the glowing edges of the leaves brighten a little and return. They are lit and clearly visible in every frame, including the first. They stay exactly the same size and shape in every frame: they never grow and never spread, only their brightness shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly.",
  "cairns/cairn_009#LIT_1#motion": "This is the same picture in every frame. ONLY the shallow water at the foot of the stones moves, rippling very slightly. It is lit and clearly visible in every frame, including the first. It stays exactly the same size and shape in every frame: it never grows and never spreads, only its inner detail shifts. Nothing is added or removed anywhere: every object appears in every frame, the same number of them, in the same places, at the same size. Everything else is copied from the first frame exactly, pixel for pixel: same shape, size, position and colour, no outline redrawn. The last frame matches the first exactly."
}
