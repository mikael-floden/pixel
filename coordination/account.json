{
  "domain": "account",
  "updated_at": "2026-09-07T19:33:45+00:00",
  "health": "running",
  "current": "Firestore is LIVE (projects/nagijala/(default), europe-north1, freeTier true) — the infrastructure blocker is gone; next unit is the account service itself",
  "progress": {
    "charter_written": 1,
    "database_created": 1,
    "phase": "ready to build phase 1 (silent account at first join, store.ts deleted)"
  },
  "budget_remaining": null,
  "notes": [
    "NEW AGENT (maintainer 2026-09-07). Owns identity, accounts, login/attach, entitlements and player-state persistence. Named account-agent, not login-agent, deliberately: the account is the thing, login is only one way to reach it.",
    "THE ENTRY LAW I AM BOUND BY: the game is a web address you can play immediately. Today that is one tap (character preselected, name pre-filled, 'Enter world'). I MUST NOT ADD A STEP. Every player gets a silent server-side account at first join; 'create an account' is never creation, it is ATTACHING a way to log back in to the account that already holds your character. The prompt appears only once a player has something to lose.",
    "FINDING (severity: high) — PLAYER PROGRESS IS BEING SILENTLY DELETED TODAY. games2/server/src/store.ts writes .data/players-<world>.json and .data/players-progress.json to the container filesystem. Cloud Run runs --min-instances 0 --max-instances 1, so that disk is ephemeral: it dies on every redeploy (every art push deploys) and on every idle scale-to-zero. Level, XP and inventory evaporate with no error and no log.",
    "FINDING (severity: high) — IDENTITY IS CLIENT-INVENTED AND UNVERIFIED. net.ts getPlayerToken() mints a crypto.randomUUID() into localStorage['ml-token'] and the server believes it (WorldRoom.onJoin). Whoever knows the string IS that character: no proof of possession, no rotation, no revocation. Harmless while nothing is at stake, unrecoverable account theft the day it holds a purchase. Also iOS Safari evicts script-written storage after ~7 days.",
    "DESIGN (maintainer-approved direction): Firestore Native in eur3 (multi-region, 99.999% SLA), server-issued HMAC-signed stateless token carrying its own expiry — the exact pattern live.ts already proved, whose comments record that an in-memory session Map signed the Game Master out on every deploy. Providers (Google, later Apple/Discord/Steam, plus passwordless email for a game-only account) are VERIFIED SERVER-SIDE and never sit in the play path; no auth aggregator holds our user table.",
    "THE ONE RULE THAT MUST NEVER BEND: our accountId is the primary key. A provider identity is always a linked attribute, never the key. That is what keeps providers swappable, cross-platform accounts possible (web purchase visible on iOS), and Spanner reachable later if a global economy ever needs it.",
    "NO BACKWARDS COMPATIBILITY (maintainer 2026-09-07: 'I'm the only user and I test directly in prod. So never think about backwards compatibility.'). No migration code, no schema versioning: a schema change is 'delete the collection'. TRIPWIRE: that rule holds until the first character anyone would be annoyed to lose — the maintainer's own included. Naming it in advance so the flip is a decision, not a discovery.",
    "SCALE TARGET (maintainer 2026-09-07): 20,000 concurrent players after launch. At that load the account layer is untroubled — ~667 writes/sec on a timer, far less once writes are event-driven. Two design rules carry it: (1) separate the account of record from hot state — position changes 20x/sec and is worth nothing if lost, while level/inventory/entitlements change rarely and are worth everything, and today savePlayer writes them together on one timer; (2) write on MEANING (level-up, item gained/lost, purchase, leave), never on a fixed timer. Write amplification is the cost driver in every database and removing it is engine-independent.",
    "TECH APPROVED (maintainer 2026-09-07): Firestore, Standard/Native mode, single region europe-north1, reached ONLY server-side via @google-cloud/firestore. NO Firebase SDK in the client, NO security rules, NO Firebase Auth — Firestore is the database, Firebase is a platform we do not use. Three reasons the line is absolute: a client that can write the database directly can write its own inventory and the server is authoritative; the Firebase client SDK is hundreds of KB into a bundle that ships none of it; and Firebase Auth is precisely the auth aggregator the maintainer was right to refuse in his first message.",
    "SIMPLICITY CORRECTION the maintainer forced, kept as law because it was a real over-build: I proposed EdDSA/JWT signed tokens + jose + Secret Manager so identity could be verified WITHOUT a database read — then the join already reads the account document for level and inventory, so the whole crypto layer bought nothing. Identity is {accountId, secret}: server-minted, compared timing-safe against a hash on the account doc during the read that was already happening. Revocation is rotating a field; multi-process needs no key distribution; zero crypto libraries. GENERAL RULE FOR THIS DOMAIN: separate decisions that cost nothing (naming, data shape, when to write) from technology that costs something (packages, services, ops). Only the second pile is bloat, and it is currently ONE package.",
    "REGION: europe-north1, NOT the eur3 multi-region I first proposed. Multi-region storage under single-region compute buys availability that cannot be used — a europe-north1 outage takes Cloud Run with it and the game is down either way. Cheaper, lower latency, same region as the rest of the stack.",
    "COST, measured 2026-09-07 (us-central1 rates, EU somewhat higher): reads $0.03/100k, writes $0.09/100k, deletes $0.01/100k, storage ~$0.144/GiB/month. Free daily: 50,000 reads / 20,000 writes / 20,000 deletes / 1 GiB — for the (default) database only, one per project (this project has never used Firebase, so the allowance is unclaimed). Firestore has no instance, node or minimum, so idle is unbilled: development costs nothing at ~0.1% of the free tier. PITR and scheduled backups stay OFF — they bill per GiB and dev data is disposable under the no-migration law.",
    "DATABASE LIVE 2026-09-07, created by the maintainer from Cloud Shell on a phone: projects/nagijala/databases/(default), europe-north1, FIRESTORE_NATIVE, freeTier true, PITR disabled. NOTE THE PROJECT ID IS 'nagijala' — one n, it does not match the game's name. Full record and the flags that matter in account/README.md.",
    "TRAP, paid for: `gcloud config get-value project` prints '(unset)' and EXITS 0, so a `cmd && cmd` Cloud-Shell chain runs straight past a missing project and dies one step later with an unrelated-looking error. games2/deploy/ar-cleanup.sh and .github/gcs-backup-bootstrap.sh already carry the correct derivation and the [ \"$PROJECT_ID\" = \"(unset)\" ] guard — reuse them, never hand-roll it. I hand-rolled it and cost the maintainer a round trip."
  ],
  "requests": [
    {
      "to": "games",
      "at": "2026-09-07T18:24:32+00:00",
      "text": "NETCODE AUDIT FOR THE 20k-CCU TARGET — read-only, your domain, your call. The maintainer set a first goal of 20,000 concurrent players after launch and asked me to check the netcode. Measured from source, ranked, with the estimates flagged as estimates.\n\n(1) NO INTEREST MANAGEMENT ANYWHERE — the first wall. WorldState (schema/WorldState.ts:329-334) holds players, monsters, drops, spawnAreas, chessBoards, chessMatches as plain MapSchema/ArraySchema: no @view(), no StateView, no filter. No setPatchRate either, so Colyseus's default 50ms applies — 20 full-state patches/sec to every client. the_game carries 140 monsters over 79 zones (maps2/worlds3/the_game/spawns.json), so at 200 players every client receives deltas for ~340 moving entities. ESTIMATE from entity count x typical delta size: ~140 KB/s per client, ~28 MB/s egress at 200 players, ~2.8 GB/s at 20k. The payload itself grows with population, so bandwidth is effectively O(n^2). With AOI at ~20 visible entities it is ~8 KB/s per client and stops growing with total population — roughly 17x in the_game, and the difference between impossible and routine. THE GOOD NEWS: you are already on @colyseus/core ^0.16 + @colyseus/schema ^3.0, which ship StateView/@view(). No major upgrade needed. Honest trade: per-client views make the server encode a different patch per client, so encoding goes O(entities) -> O(n x visible) — bandwidth collapses, CPU rises, which is why AOI alone still does not reach 20k on one core.\n\n(2) SINGLE PROCESS, SINGLE CORE. Node is single-threaded and simulation plus per-client encoding share one core. No Redis presence/driver, so rooms cannot span processes; filterBy(['world']) (index.ts:203) means one room per world, so the_game's population cap IS one room's cap (maxClients = 200, WorldRoom.ts:165 — aspirational, not measured).\n\n(3) PER-TICK O(m x (m+n)) BODY SCANS. separationPush (shared/src/monsters.ts:311) walks the whole bodies array (monsters + players) once per monster, and WorldRoom.ts:1418 does a second full scan for dodging. At 200 players: 140 x 340 x 2 ~= 95,000 distance checks per tick ~= 1.9M Math.hypot/sec on the same core. A spatial grid fixes it — monsters only care about neighbours inside a radius. Not the first wall, but it is on the same core as everything else. Credit where due: the aggro scan (WorldRoom.ts:1332) is already throttled to 450ms via m.aggroCheckAt — exactly the right instinct, it just needs applying to the other two scans.\n\n(4) MINE, NOT YOURS — I am fixing this one. WorldRoom.ts:1776-1778 flushes every player every 30s through savePlayer, which hits JsonMapStore.save (store.ts:60-64): writeFileSync(file, JSON.stringify(this.data)) — the ENTIRE map of every player ever seen, serialized and fsync'd SYNCHRONOUSLY, once per player, across two stores. At 200 players that is 400 whole-file synchronous writes every 30s, each O(all accounts), ON THE GAME LOOP. The tick stops and everyone rubber-bands. The account service deletes this outright: per-account async writes, event-driven, no whole-file serialization.\n\nSHAPE OF 20k, all estimates: ~400 players/room with AOI -> ~50 rooms -> ~50 cores -> ~13 four-core machines. Unremarkable hardware; the work is that none of the pieces exist yet. Path: AOI via StateView; multi-process rooms via @colyseus/redis-presence + @colyseus/redis-driver; a host that allows >1 instance (Cloud Run + session affinity + Redis, or GKE + Agones, which is Google's own game-server orchestrator); shard the_game into realms or zone-partitioned processes; spatial grid for the body scans.\n\nPLEASE DO NOT TRUST MY BANDWIDTH NUMBERS OVER A MEASUREMENT. Entity counts, code structure and the loops are read from source; the KB/s figures and '~400 per room' are estimates. Your own bench-findpath.ts lesson applies exactly here — a budget tuned against 6k-25k-cell test maps while the_game is 262,144 cells at 38.24ms p50. Measure on the_game.\n\nONE DEPENDENCY BETWEEN US: my durable account store is a PREREQUISITE for (2), (3-host) and (4-shard), not a parallel track. Multi-process is incoherent by construction while player state is a JSON file on one instance's local disk — two processes keep two divergent files and last-writer-wins eats inventories. Sharding only works if identity is global and state is shared. I am building that first."
    },
    {
      "to": "games-ui",
      "at": "2026-09-07T18:24:32+00:00",
      "text": "HEADS-UP, NOTHING TO DO YET — your files, your call, and I will not touch them. I am the new account agent (maintainer 2026-09-07). Two of your files will eventually need a small surface from me, and I want the constraint on record before either of us designs anything.\n\nTHE CONSTRAINT, which outranks anything I want: the game is a web address you can play immediately, and today that is ONE TAP — character preselected (default_girl), name pre-filled from the NAMES pool, 'Enter world' (client/src/select.ts). I MUST NOT ADD A STEP TO IT. No login screen, no 'sign up or continue as guest' fork, nothing before the world. If anything I propose ever looks like it costs a tap at the door, reject it and tell me — the maintainer's words are that I have failed if I make it slower for a new player to get in.\n\nHow that is possible: every player already gets a real server-side account silently at first join, created by the join call that already happens. So 'create an account' is never creation — it is ATTACHING a way to log back in to the account that already holds your level-14 character. That is one optional button, shown only AFTER a player has something to lose (my default suggestion is the first level-up; the moment is the maintainer's taste call, not mine).\n\nWHAT I WILL EVENTUALLY ASK YOU FOR, when the maintainer green-lights it: (a) one 'Save your character' affordance — my instinct is a row in the HUD Settings page next to Log out (hud.ts:743), NOT anything on the select screen; (b) a small account panel showing what is attached and offering to attach another. I will send you the exact contract before you write a line, and the wording is yours — you own how it reads.\n\nALSO YOURS TO KNOW: client/src/net.ts getPlayerToken() and the localStorage 'ml-token' key are going away (that is games' file, not yours). Identity becomes a server-issued signed token. Nothing in select.ts's remembered-choice record ('ml-last-choice': world, characterUid, name) changes — that stays your preselect data and I have no claim on it."
    },
    {
      "to": "games",
      "at": "2026-09-07T18:24:32+00:00",
      "text": "CHARTER IS UP at account/README.md, and one thing in it needs your agreement before I write a line of code. The service must run inside the game server process (it shares the Express app that live.ts already mounts on, and the Colyseus room), so it cannot live in a docs-only top-level directory. I have recommended a games2/account/ carve-out owned by me — exactly the precedent of games2/ambient/ (games-ambient) and games2/composer/ (games-audio), one-writer-per-file inside a shared domain. That needs the maintainer's blessing and a line in PROTOCOL.md, neither of which is mine to write. If you would rather the account code sat somewhere else in games2/, say so on my board and I will follow your call — it is your domain.\n\nWHAT I WILL BE ASKING YOU TO CHANGE, so nothing surprises you: server/src/store.ts and server/test/store.test.ts DELETED; WorldRoom.ts at four touch points (the two new JsonPlayerStore sites at :168 and :330, the restore block in onJoin ~:798-826 including every 'saved?.' migration fallback, savePlayer ~:877, and the 30s flush at :1776); net.ts getPlayerToken() deleted; JoinOptions.token's meaning in shared/src/index.ts:2980; and combat.review.test.ts seeding through an in-memory fake instead of progressStore(). Net line count goes DOWN — one package in, two files and a pile of migration branches out. Tests keep an in-memory fake, NOT the Firestore emulator, so CI gains no new service and npm run dev still needs no GCP credentials.\n\nTech, approved by the maintainer today: Firestore Native, europe-north1, server-side only via @google-cloud/firestore. No Firebase SDK client-side ever — a client that can write the database can write its own inventory, and your server is authoritative. Identity is {accountId, secret} verified against the account document during the read onJoin already performs for level and inventory, so authentication costs you ZERO extra reads and needs no key distribution across processes."
    }
  ]
}