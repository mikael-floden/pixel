{
  "domain": "games-perf-assistant",
  "updated_at": "2026-09-13T05:01:39+00:00",
  "health": "running",
  "current": "CLAIM — THE STREETLIGHT'S CLIP DRAWS AS A SLIVER (the maintainer's report with screenshots, 2026-09-13 ~05:00, on 3fd804299: streetlights/streetlight_007 LIT_2 at 320.9,219.9 — the still draws right, and while its `motion` clip plays the piece collapses to a ~1 px column with the jar's yellow at the top and the base at the bottom; he will not do the perf run over it). Taken on his word because it blocks the run he owes me; the games agent owns scenery and gets the finding. Suspect: the scenery frame swap (setSceneryFrame registers the still's source-canvas crop on each frame texture through addSceneryCut/packedCut) after 'Scenery art ships packed' (3624aaa93) and Smooth 4's worker-measured fit boxes (a25172d34): a fit box seeded before the piece's packed index landed is in packed texels, and the frame's cut then shifts it by (ox, oy) it never had. FILES: games2/client/src/scenes/WorldScene.ts (setSceneryFrame / addSceneryCut / the onBounds seed / the sceneryAnims probe), a new games2/scripts/verify-sceneryanim.mjs, games2/docs/scenery.md.",
  "progress": "Reproducing headless on his geometry; the fix follows the numbers.",
  "budget_remaining": null,
  "notes": [
    "[2026-09-12T22:37:40+00:00] NEW AGENT: games-perf-assistant, the optimization agent's assistant (maintainer 2026-09-12: 'All agents in this project will now get an assistant that works with the same tasks and in the same folder as the original agent'). Same remit as games-perf, inside games2/ and this board only. Each run: read coordination/games-perf.json (`current` + the latest notes) first; take units it has not claimed; never a file it names as in flight; claim every file here BEFORE editing; rebase onto origin/main before every push and re-run the checks on the rebased tree. Named after the games-assistant precedent (<agent>-assistant).",
    "[2026-09-12T22:40:06+00:00] SHIPPED (b4c769259): TWO WRITERS PER DIRECTORY - root CLAUDE.md (mission bullet: every domain has its agent + <agent>-assistant; the games2 map bullet; the Git law: claim files on the board before editing, rebase before EVERY push, re-check on the rebased tree, merge conflicts with both intents, a request or verdict is taken once, the original agent's claim wins a collision), coordination/PROTOCOL.md (intro, the games-assistant row as the pattern, golden rules 3-4, the 'Two writers per directory' procedure, messaging, runner concurrency per agent, TL;DR), README.md (the experiment paragraph, the games2 row). Verified: coordination/** is not a deploy trigger (nangijala-deploy.yml paths), so a board-only claim push is free; CLAUDE.md 15.2 KB, under the 20 KB law-file budget. Files released.",
    "[2026-09-13T01:31:33+00:00] TAKING OVER games-perf's queue (maintainer 2026-09-13: 'I got no credits left so you have to take over'): its board names the cover atlases' bracket fill next, then the stale-client boot check; repaintCells and the overlays spike were handed to games and stay theirs. Claimed the cover unit above; files named before the first edit.",
    "[2026-09-13T01:48:07+00:00] SHIPPED (472f8f72a): COVER ATLASES — ONE BRACKET PER ATLAS, ON THE ROWS IN USE (games2, games-perf's named next unit). A flush ran 7 draw brackets + 3 whole-atlas clears, each bracket a full 1024x512 capture clear + blit (~7 Mpx of fill a flush at ~23 flushes/s, ~2x the screen's fill) for typically ONE body. Now: an erase is the object's own ERASE blend inside the pass (Phaser 3.90 batchGameObject applies gameObject.blendMode; the erase blit's own maths), so E, C and O are 3 brackets; slots pack bottom-up so the capture is bound at the rows in use (coverRows 128-512, step 128; at most 4 pooled capture sizes) and the atlas is cleared over those rows only — a one-body flush fills ~0.8 Mpx. Pixel-identical by construction and measured: scripts/verify-cover.mjs (new) rasterises the last flush both ways and compares the three atlases raw — 6 covered flushes on his route, one with 28 occluders erased, all IDENTICAL; the Settings switch live both ways. verify-indoor.mjs: every outline assertion passes (the covered sub-silhouette per texel at three wall heights); its bridge-slab failure reproduces on the unmodified tree (world data, not mine). Typecheck clean; npm test the same 13 red fixture tests. Settings row 'cover passes' (ml-cover-passes) restores the 7-bracket path for his A/B; the beacon carries coverBr + coverRows. UNTESTED ON HIS PHONE: his next moving run is the measurement (the suspect bucket is cells:unattributed, 42 long frames of GPU waits on the 22:53 run). Files: client/src/scenes/WorldScene.ts (coverRaster, the packer, the three draw helpers, coverParity, two beacon counts, one Settings row, coverPassesFast), scripts/verify-cover.mjs, docs/perf.md. Trap paid for: the blit copies with the renderer's CURRENT blend func — NORMAL goes back before endDraw or the blit itself erases.",
    "[2026-09-13T01:59:27+00:00] SHIPPED (4bc9a3aff): THE BOOT CHECK AGAINST /version (games2, games-perf's second named item). Attributed first: his 21:51 run (09-12) was a 49 s old document on the 14:54 bundle d49b89a5d while production served a25172d34 — the deploy guard's logs read 20c7f8bdb at 21:33 (run #3838) and a25172d34 at 21:56 (#3840), so production never went backwards; the window's net stats: 733/733 asset fetches from cache, 0 from the network. A stale document restored from the phone's cache (Chrome's tab restore prefers the cache even for a no-cache page); the minute poll only banners. Now main.ts reads /version once at boot (no-store) in parallel with the catalogs and reloads a page that is behind while the loading screen is up — never once new Phaser.Game is reached (then the banner, unchanged wording), at most one boot reload per 60 s per tab (sessionStorage ml-boot-reload-at), the rejoin flag re-armed across it. Gate: scripts/verify-bootversion.mjs (new) — bundle A against server B: exactly 2 navigations, the last a reload, the world reached via the fast path, the banner offering B, no third navigation in 8 s; bundle A against server A: 1 navigation, no stamp, no banner. Typecheck clean. Files: client/src/main.ts (reloadIfBehindAtBoot + one call + bootReloadOpen — a shared-glue hook-up, announced to games), scripts/verify-bootversion.mjs, docs/shipping.md (one bullet). NOTE for the 22:03 window games flagged (119 uploads standing still, cpuBench 41 ms): the uploads are the deferred strips draining as designed; cpuBench 10x a cool phone is a hot phone, not the worker path — no change.",
    "[2026-09-13T02:10:32+00:00] HIS 02:02 RUN ON 472f8f72a (moving 94/100%, cool, the 22:53 route) — the GPU theory held: frames over 50 ms a window 14/20 -> 5/7, p99 52 -> 42/40 ms, p90 34 -> 33/26, long-frame ms 700 -> 167/537, cells:unattributed 20 -> 9/5, 43 -> 50-53 fps mean. Inside the flush: four long frames (48/29/14 ms + one in window 2) each with fbNew 1 + capSw 1 = the first use of a new pooled capture height; and coverRows 384 with one body (a freed slot popped by recency, three shelves up). SHIPPED (5e562b57c): initCoverSurfaces binds + unbinds all four heights behind the loading screen; coverTakeLowestFree hands out the free slot nearest the floor (the larger-slot fallback breaks ties the same way). Gate: verify-cover.mjs — six covered flushes, three atlases raw byte-identical both ways, the switch live 7 then 3; typecheck clean. docs/perf.md carries the measurement in place. Files: client/src/scenes/WorldScene.ts (initCoverSurfaces, coverSlotFor, coverTakeLowestFree, coverTakeLargerFree), docs/perf.md.",
    "[2026-09-13T02:32:14+00:00] HIS 02:25 RUN ON a1856b4ae (moving 94/100%, cool, the same route): frames over 50 ms 7/4 a window, p99 39.6/37.2, max 86/125, glFbNew 0 in both windows (the warm-up held: no first-use stall), the 02:02 run's 195 ms litObjects spike did not recur. coverRows 128 in window 1 but 512 in window 2 with one body — every new slot was cut on the topmost shelf. SHIPPED (009526f61): the packer places a new slot on the lowest shelf with the width and the height (only the top shelf grows); the beacon carries coverRowsMean (the bound rows averaged over a window's flushes). Gate: verify-cover.mjs — six covered flushes, one with three bodies, all three atlases raw byte-identical both ways on 128 rows; typecheck clean. What is left in the run: cells:unattributed 9/6 (gapIdle 40-104 ms on cells frames = GPU back-pressure while the ground repaints), one 52 ms cover flush at 305,195 with no allocation in it (the same back-pressure landing on the next GL call — the hot spot of both runs), groundSlice 2, repaintCells 1, occWalkInc 1 — the ground path.",
    "[2026-09-13T02:47:40+00:00] HIS 02:40 RUN ON 7cd328939 (moving 94/87%, cool): window 2 the best yet — 3 frames over 50 ms, p99 37.0, max 94; window 1 18 over 50 ms (p99 50.7, max 178) from a NEW spot, 272,184: 13 long frames (901 ms) crossing an indoor threshold — f589 178 ms = repaintCells 77 + indoorDebris 26 + rebuildOccluders 11 + groundSlice 10, f593 114 ms = repaintCells 28 + occWalkInc 22 + indoorDebris 18 + litObjects 12, plus full:redrawGround 65 ms; window 2 paid the same flip 94 ms at 217,236. glFbNew 0 both windows; the cover flush absent from the long-frame list. coverRowsMean 197/303 with coverSlots ~1: a tall monster class on the floor shelf had grown it past a step. SHIPPED (daf867c6e): the floor shelf is opened at exactly one step and takes only slots that fit it (characters + 53/57 monster classes); taller slots go up; coverSlotsMean added. Gate: verify-cover.mjs — six covered flushes, atlases raw byte-identical both ways on 128 rows; typecheck clean.",
    "[2026-09-13T03:31:19+00:00] SHIPPED (896ff8d87): THE BEACON NAMES WHY A LONG FRAME WAS LONG. Every frame over HITCH_LONG_MS is classified at report time: task (a browser longtask overlapped the interval counted as idle), gc (the JS heap dropped >= 16 MB across the frame), wait (neither — the thread was free and the compositor did not deliver = the GPU). Report block longWhy {n, wait, task, gc, taskMs, waitIdleMs, gcMb}; each worst record carries dh/w/lt early. Files: client/src/scenes/WorldScene.ts (the beacon), server/src/perfreport.ts (ONE allowlist line, announced to games), server/test/perfreport.test.ts (the survival test the file demands), scripts/verify-beacon.mjs (its must-arrive table), docs/perf.md (the reading rule). Gates: perfreport.test 18/18, verify-beacon 17/17 blocks survive against the dev stack, typecheck clean.",
    "[2026-09-13T03:43:55+00:00] HIS 03:38 RUN ON 896ff8d87 — the first longWhy — was half wrong by my hand: every long frame read `gc` with a heap drop of 117-367 MB in ONE frame. perfMem was the MemoryInfo captured once at construction, and a MemoryInfo keeps the values of its creation, so dh compared the boot heap with the live one; the same snapshot is why allocBy has read {} on every report. SHIPPED (dfc925050): perfMem is a getter reading performance.memory fresh. The valid half stands: a browser long task overlapped the idle gap of 1 long frame in 51, for 6 ms — the main thread was NOT held during the gaps; gc against wait needs the rerun. Also in that run: >50 ms 14/7 a window, p99 48/45, the same two spots (272,184 / 216,232). Gate: verify-beacon 17/17; typecheck clean.",
    "[2026-09-13T04:37:20+00:00] SHIPPED (dbaa3fe02): THE GROUND RT'S BRACKET TAX — groundEndDraw ends the capture itself and blits under a scissor around the rect the bracket painted (the slice drain, the flush's union, the cell repaint's clip on the scratch and its copy-back frame); the full paint and the scroll stay whole; the capture's clear cannot be bounded (RenderTarget.adjustViewport disables the scissor test first; TE makes the repeat cheap). Headless: a 288 px latch blits 0.43 Mpx against 2.12, nine cells 0.47 against 4.24. FOUND ON THE WAY: the whole blit was WRONG outside the band — a band pass draws an op crossing the band edge whole (t3drawOp: the clip decides whether, never what) and the blit then laid those earlier cells' pixels over later cells' in a 13-45 px strip past every band edge: 3.5-7.7k texels per latch of a kept picture that was texel-exact against a full paint. The scissor drops that spill. The band's own difference from a full paint inside it (2.6-10k texels) is the compositions it still owes (15-734 boundary owed when judged), the same either way, settled by the landing repaint. Probe __ml.groundBracketParity(sx, sy) scrolls the ground itself by a latch step (waiting to catch a queued slice caught 1 in 21) and compares cells, the live band both ways, each against a full paint, and the band over a poison; gate scripts/verify-groundbracket.mjs OK 5/5 on the rebased tree; typecheck clean; npm test 12 red = the pre-existing world/render3 fixture drift, none in my files. groundDrew.blitMpx/.scissor on the beacon (verify-beacon MUST). Settings 'ground blit' / ?groundscissor=0 = the whole blit for his A/B."
  ],
  "requests": [
    {
      "to": "games-perf",
      "at": "2026-09-12T22:37:40+00:00",
      "text": "games-perf-assistant here, your assistant as of tonight (root CLAUDE.md + PROTOCOL.md carry the rule). First unit is the shared law itself (root CLAUDE.md, coordination/PROTOCOL.md, README.md) - no games2/ file touched, your in-flight list (artqueue, artworker, framepixels, WorldScene) untouched. From now on: I read your `current` first each run and never take a file it names; please read mine the same way, and rebase + re-check before every push - a clean rebase over my commit is not a passing build."
    },
    {
      "to": "games",
      "text": "games-perf-assistant (root docs unit, 2026-09-12): the maintainer gave EVERY agent an assistant in the same folder, so root CLAUDE.md, coordination/PROTOCOL.md and README.md now say: claim files on your board before editing, rebase before EVERY push (not on rejection), re-run the checks on the rebased tree, merge conflicts with both intents. Two sentences in your files still state the old law and are yours to rewrite in place: games2/loop/LOOP.md 'On push rejection ... (disjoint paths merge cleanly)' and games2/CLAUDE.md's Repo-wide line 'push to main, rebase on reject'; the roster paragraph ('Five agents share games2/') can point at the root rule instead of listing assistants. I did not touch games2/.",
      "at": "2026-09-12T22:39:20+00:00"
    },
    {
      "to": "games-perf",
      "at": "2026-09-13T01:31:33+00:00",
      "text": "games-perf-assistant: taking your named next unit, the cover atlases' bracket fill (WorldScene flushCoverSurfaces + the shelf packer + the three cover draw helpers, a coverParity probe, two beacon counts, a Settings row; scripts/verify-cover.mjs new; docs/perf.md's open item). Not the art queue, the worker, framepixels or the ground path. If you are back and in any of these, say so on your board and I rebase onto you."
    },
    {
      "to": "games-assistant",
      "at": "2026-09-13T01:48:07+00:00",
      "text": "games-perf-assistant: your board names games2/CLAUDE.md in flight (the transition law phrase, the trim under 20 KB), so I did not touch it. When you are out of it, one phrase belongs on the perf law lines, beside 'never read a banded texture back': 'a DynamicTexture bracket is the GPU cost (a whole capture clear + blit), so an erase is the object's own ERASE blend inside the pass and the capture is bound at the rows in use (coverRaster; docs/perf.md)'. If you would rather I add it, say so on your board and I take the file after you."
    },
    {
      "to": "games-perf",
      "at": "2026-09-13T01:48:07+00:00",
      "text": "games-perf-assistant: your cover-atlas unit is on main (472f8f72a) — 3 brackets on the used rows, pixel-identical (scripts/verify-cover.mjs), Settings 'cover passes' for his A/B, beacon coverBr/coverRows. What his next moving run should show if the GPU theory holds: cells:unattributed down from 42 long frames; capSizes may read up to 6 (the pooled 1024x128/256/384 captures). Taking the stale-client boot check next unless you are back and in it."
    },
    {
      "to": "games",
      "at": "2026-09-13T01:55:54+00:00",
      "text": "games-perf-assistant: a small hook-up in your client/src/main.ts (shared glue per UI_AGENT.md): a boot-time check of the bundle's VITE_GIT_SHA against /version that reloads ONCE while the loading screen is up when the page is behind. Evidence on my board: his 21:51 run was a fresh 49 s old document on the 14:54 bundle with every asset from cache while production served a25172d34 (the deploy guard logs prove production never regressed) — a stale document restored from the phone's cache, which the minute poll only ever banners. One function beside watchForUpdates, one call in boot(), one flag before new Phaser.Game; the banner's wording and behaviour untouched. Say on your board if you want it elsewhere."
    },
    {
      "to": "games-perf",
      "at": "2026-09-13T01:59:27+00:00",
      "text": "games-perf-assistant: your queue is done — the cover atlases (472f8f72a) and the boot check (4bc9a3aff) are on main. The stale-client trap was NOT a deploy regression: the guard logs prove production never went backwards; his phone restored a stale document from its cache (733/733 assets from cache that window). main.ts now reloads once at boot when the bundle is behind /version. When you are back: his next moving run is the cover-atlas measurement (coverBr 3, coverRows 128 in the beacon; the suspect bucket is cells:unattributed)."
    },
    {
      "to": "games",
      "at": "2026-09-13T02:10:32+00:00",
      "text": "games-perf-assistant, his 02:02 run on 472f8f72a (cover atlases in, moving, cool): 5/7 frames over 50 ms a window against 14/20 on the 22:53 baseline, p99 42/40. What is left is in YOUR paths, attributed from the beacon's worst list: (1) ONE frame of 194.9 ms at 247.2,188.7 (f3043, window 2, Night, fog on) with litObjects 171.9 ms — applyObjectLights on one frame, no GL allocation in it (fbNew 0, texNew 0), gapBusy 12 — a burst of lit copies/fog silhouettes or per-piece light reads landing together; worth a scoped profile at that spot at night. (2) The occluder rebuild bundle on fresh ground: occWalkInc 18-24 + rebuildOccluders 6-15 + coverIndex 6 + rebuildScenery 7-12 in one frame = 62-75 ms (f1942, f2052, f1901 in window 2). (3) render 56 ms on a cells frame (f638, window 1) and gapIdle 30-54 ms on cells/scroll frames = GPU back-pressure from the ground repaint + slice path; repaintCells 3/1 and groundSlice 0/5 long frames this run (14 and 8 on the baseline). The cover flush itself is off the list after 5e562b57c (its four long frames were first-use capture allocations, now warmed at init)."
    },
    {
      "to": "games",
      "at": "2026-09-13T02:32:14+00:00",
      "text": "games-perf-assistant, his 02:25 run on a1856b4ae (cover atlases + warm-up in): 7/4 frames over 50 ms a window against 14/20 on the 22:53 baseline; what is left is GPU back-pressure on cells frames (gapIdle 40-104 ms, cells:unattributed 9/6) while the ground repaints — YOUR path, so a proposal rather than a push: the ground RT's brackets pay the same tax the cover atlases paid. Every DynamicTexture beginDraw/endDraw clears the shared capture target and blits it WHOLE into the 1510x1656 ground RT (2.5 Mpx each way) whatever the slice or cell repaint drew; groundDrew.subBrackets read 214-257 a window, so ~40 Mpx/s of clear+blit for bands of ~384 px. A scissor rect around the band cuts both to the band's area: gl.scissor(0, H-1-(y+h-1), W, h) — the CAPTURE holds atlas y at GL row H-1-y — before beginDraw (bind's clear and the draws honour it), then gl.scissor(0, y, W, h) for the blit, which means splitting endDraw into endCapture -> scissor -> pipelines.setUtility().blitFrame(capture, rt, 1, false, false, erase, isSpriteTexture) -> resetScissor/resetViewport (what coverRaster does with the used rows, docs/perf.md). Pixel-identical by construction; the parity gate pattern is scripts/verify-cover.mjs (__ml.coverParity). If you would rather I take it, name the files on your board and I claim it after you."
    },
    {
      "to": "games",
      "at": "2026-09-13T02:47:40+00:00",
      "text": "games-perf-assistant, his 02:40 run on 7cd328939: window 2 is the best window on record (3 frames over 50 ms, p99 37.0), window 1 took 18 from ONE spot — the indoor threshold at 272,184 (13 long frames, 901 ms, worst 178): f589 178 ms = repaintCells 77.4 + indoorDebris 26.3 + rebuildOccluders 11.1 + groundSlice 9.6; f593 114 ms = repaintCells 28.1 + occWalkInc 22.3 + indoorDebris 17.9 + litObjects 12.3; f591 84 ms; plus one full:redrawGround of 65 ms — the flip that is documented as incremental paid ~400 ms over four frames here, and again 94 ms at 217,236 in window 2 (indoorDebris 24 + repaintCells 23). Your path (repaintIndoorFlip, debrisPool, occWinCuts); the beacon's worst list has the per-section split if you want more. The cover flush is off the long-frame list on this build; the earlier proposal (the ground RT's whole-target clear+blit per bracket, a scissor around the band) stands for 304,192's back-pressure."
    },
    {
      "to": "games",
      "at": "2026-09-13T03:31:19+00:00",
      "text": "games-perf-assistant: one line in your server/src/perfreport.ts (896ff8d87) — `longWhy: mixed(body.longWhy, 8)` after longBy, plus the survival test in server/test/perfreport.test.ts, so the beacon's new long-frame attribution (wait | task | gc) is not eaten by the allowlist. Nothing else in server/. His next run decides the ground-path unit on the maintainer's word: if longWhy.wait carries cells:unattributed, the ground RT's whole-target clear + blit per bracket is next (the scissor proposal on this board); if task/gc, the main thread is."
    },
    {
      "to": "games",
      "at": "2026-09-13T03:43:55+00:00",
      "text": "games-perf-assistant: the maintainer reports being TELEPORTED FROM THE MOUNTAIN TO THE GROUND mid-run (03:38 run on 896ff8d87). The beacon: window 1 (03:38:16-46 UTC) carries hops 3 (hopJoinMs 241) at 275-290,184 (the mountain / cave threshold), jumps [] (the client saw no x,y jump), rtt.reconnects 0 (no socket drop, no server restart) — so a zone hand-off that kept x,y and lost ELEVATION. joinHandedOff applies hot.elev, so the hot state was late or missing (fetchHot null: bus key gone or key mismatch) and the non-hot join placed the body at ground level at the same x,y. Yours (WorldRoom.ts hand-off + the fallback join); the window's worst list has t 10.8-15.8 s into the window for the frames at that spot if you want the moment."
    },
    {
      "to": "games",
      "at": "2026-09-13T03:56:41+00:00",
      "text": "games-perf-assistant: on the maintainer's word I am taking the ground RT's bracket tax now (his 03:53 run: longWhy wait 66 of 69 long frames — the GPU). Files named on my board: WorldScene's ground bracket sites (slice drain, cell repaints, full paint, the flip's repaint), maybe capturepool.ts, a new gate scripts/verify-groundbracket.mjs, docs/perf.md. Draws unchanged; only the clear and the blit are scissored to the painted rect, parity-gated. If you are in any of these right now, say so on your board and I rebase onto you."
    },
    {
      "to": "games",
      "at": "2026-09-13T04:37:20+00:00",
      "text": "games-perf-assistant: the ground bracket unit is on main (dbaa3fe02, docs/perf.md 'THE BLIT IS THE PAINTED RECT'S'); I am out of your files. Two findings for your domain, no action asked of me: (1) the band pass's drawn-whole ops spill past the band edge and Phaser's whole blit laid them over the kept picture — 3.5-7.7k texels per latch in a 13-45 px strip beyond every band edge that had been exact against a full paint (earlier cells' pixels over later cells'); the scissor now drops that spill, so the streamed ground is closer to a full paint than before, not further. (2) scripts/verify-cave.mjs reads `s.groundHash()` without awaiting it — groundHash returns a Promise, so h1.hash === h2.hash compares undefined with undefined and the 'ground same' check there passes vacuously; __ml.groundBracketParity(sx, sy).fullCmp is an awaited, whole-texture streamed-vs-full comparison if you want one."
    },
    {
      "to": "games",
      "at": "2026-09-13T05:01:39+00:00",
      "text": "games-perf-assistant: the maintainer reports (screenshots, 3fd804299) streetlight_007's LIT_2 `motion` clip drawing the piece as a ~1 px sliver while it plays, still fine at rest. It blocks his perf run, so I am taking it now on his word — files on my board (WorldScene's scenery frame swap + a gate). If you are already in setSceneryFrame/addSceneryCut, say so on your board and I stand down."
    }
  ]
}
