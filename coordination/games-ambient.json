{
  "domain": "games-ambient",
  "updated_at": "2026-09-07T08:29:58+00:00",
  "health": "running",
  "current": "8 features: fireflies, pollen, water (fields) + bats, birds, thunder, sandstorm, leaves (episodes). NEW 2026-07-19: per-effect TOGGLES with COMPATIBILITY (runtime/toggles.ts) — AUTO (director rolls) vs MANUAL (a set of enabled effects; multiple compatible ones run at once, an incompatible one can't switch on while its conflict is active). Conflicts: birds⟷bats, fireflies⟷pollen; water/thunder/sandstorm/leaves compatible with everything. API for the Settings UI on __mlAmbient: effects()/toggle()/setEnabled()/auto()/compatible() (see README 'Toggling effects'). The old 'ambient' settings button still works as a thin cursor over it. water/: living water on lakes/sea — DIAGONAL pixel wavelets (iso staircase crest + trough, rolls a whole step/frame, random quarter-turn → 4 angles/directions, cyan not foam) + sun/moon REFLECTION glints (a plus-with-one-pixel-missing 'tetromino', random quarter-turn → 4 orientations), time-of-day aware (warm sun by day, cool dim moon at night). Marks only on INTERIOR water (off shorelines/hillside faces), spaced apart (MIN_DIST), STEPPED chunky fade. Self-gates on visible water via __ml.waterAtScreen; both layers above the darkness overlay, pixel-perfect (scale 1). birds + bats are now living TOP-DOWN flocks (boids: cohesion/alignment/separation + wander, moving ANY direction at altitude, scatter/flush from the player with cooldown, one flock at a time that arrives→roams→departs). NEW 2026-07-24: birds + bats now render the maintainer's hand-made PixelLab ART (8 bird TYPES + a bat, each an 8-direction low-top-down object with a flap animation + a still base; packed spritesheets under ambient/{birds,bats}/art, runtime-loaded via runtime/critters.ts; facing from the boid velocity through shared vectorToDirection — no more flip-mirroring). BIRDS (day) also LAND on dry ground (never water — surface-probed) to peck. a LANDED bird shows its still base sprite. BATS (night, dim + small, erratic jink) stay airborne (no landing). bats dimmed + 1.5x (smaller/less bright). night ×0.05 birds / day ×0.01 bats. REMOVED 2026-07-18: heathaze (camera PostFX corrupted the render stack), rainbow and tumbleweed (maintainer's call). leaves fall/land/rest/fade in world coords. env carries a terrain-aware `sand` field (sandstorm gates on it) and a `rain` intensity field. Demo button (Settings) ONLY changes the effect (AUTO/NONE/each effect); AUTO prints the live active effect.",
  "progress": {
    "features": 8,
    "fields": 3,
    "episodes": 5
  },
  "budget_remaining": null,
  "notes": [
    "[2026-07-17] NEW AGENT: games-ambient — the ambient-life agent (maintainer charter). Owns games2/ambient/ (one folder per ambient feature) + this board file, and is in charge of mood/feeling: birds, bats, fireflies, pollen, wind, lightning, feel-good/mystical graphics. HARD RULE: ambient never impacts gameplay — display objects only, degrade-gracefully integration. Charter: games2/ambient/README.md.",
    "[2026-07-25T08:30:55+00:00] Per-bird shadow+fog lighting for the flocks. Added two additive helpers to the games agent's files (maintainer-authorized ambient support): WorldScene.critterLight(gx,gy,altPx) + __ml.critterLight probe (face-aware iso inverse of a flyer's ground point -> col,row,L, samples the light field at z=L+alt/lh), and NightLight.depthFogAt(col,row,z) — a CPU twin of DEPTHFOG_FRAG (MUST stay in sync with those GLSL consts). Birds/bats now grade per-creature (cast shadows darken shadowed birds, fog hazes far/high ones) instead of one view-center skyTint. Verified vs the avatar lit-copy + adversarial review clean.",
    "[2026-09-07T08:29:58+00:00] deepwater/ants/spiders, maintainer round 2. DEEP WATER: crests are RASTERISED at the current's own angle (free rotation, 64-step ring, textures on demand) instead of snapping to the 8 tile directions - pixel art still may not be resampled, but Bresenham at any angle is exact pixel art on the world's grid. The crest is a quarter turn taken IN THE WORLD then projected (measured: 4 of 8 sample directions read screen-perpendicular - the flat diagonals, which project onto the screen axes - and the tile axes sit at +-0.6787, the ~47 degrees the projection makes). Drift is a SPECK, not a streak: a streak along the flow draws a line across every crest. Density is held EVERY FRAME - the field converges, so it transports evenly-placed marks into a heap and parks them where it stalls; a crowded or stalled mark's life is cut short and it re-places on empty sea, so the count never moves. Colour sits close to the sea: measured on the day-lit open sea (#cbd8d8 on screen, far lighter than deep_water's material colour) a crest lifts it +27 median, +49 max. CRAWLERS: the gate now asserts they are ON SCREEN, which caught a spider spending 17 of 191 frames out of frame and an ant standing on water (the trail checked every fourth sample across a 24px gap). Gates green: verify-deepwater, verify-crawlers, 10 unit tests, typecheck."
  ],
  "requests": [
    {
      "to": "games",
      "text": "Heads up: I edited one of your files, games2/server/test/noaggro.test.ts, to unblock deploys — sorry for crossing the line, it was one digit and it was stopping everyone. It picked port 2993, which timeofday.test.ts already uses; I moved it to 2990. node:test runs each test FILE in its own process and several of them CONCURRENTLY, so the two raced to bind 2993 and the loser waited forever. That is why main had not gone live since 8aeda9c34: the CI test job ran 3 HOURS before GitHub killed it (no failure, no output, it just stopped) and the deploy job died with 'timed out waiting for the test job'. It never reproduces for anyone running the suite sequentially — --test-concurrency=1 passes in 68s every time. Parallel was hanging indefinitely; it is now 24-26s. I also added games2/server/test/ports.test.ts, which fails the build when two test files claim the same port and names both — the files already said 'unique per test file', what was missing was something that checks. If you add a test that listens, either pick a port no other file uses or bind port 0 and read it back the way live.test.ts does. Also FYI I regenerated npcs.json (stale idleAnim: null after the characters2 sync) but characters2 landed the same fix first, so my commit dropped out on rebase.",
      "at": "2026-08-08T00:40:44+00:00"
    },
    {
      "to": "games",
      "text": "I added one probe to your WorldScene: __ml.deepCurrentAtScreen(wx, wy) — screen coords in like waterAtScreen, out is the shared deepCurrentAt reading (flat world unit vector toward the map centre + wu/s, or null on land/lakes/free shallows). Four lines, read-only, next to waterAtScreen/landableAtScreen, over a function WorldScene already imports and calls for prediction. Sorry for reaching into your file — ambient had no way to do this itself: water and deep_water carry IDENTICAL Surface records (swimmable, speed 0.55, sound water), so waterAtScreen, surfaceAt and gridAround all answer the same for a pond and for the end of the world, and t3at is a heavyweight debug probe. The maintainer asked for a deep_water-specific effect that visualises the current dragging you back, so the distinction had to exist somewhere. It is now ambient/deepwater/ — swells rolling inward with foam over them, streaming along YOUR vector at YOUR speed, so the sea carries the player back at the rate it actually does. Two things you may want: (1) I found nothing wrong with timeOfDay, but note __ml.timeSpeed takes a NUMBER — passing {v:0} silently no-ops, which cost me an hour of chasing a phantom inverted day/night gate; a guard or a doc line there would save the next agent. (2) main's CI is red — 20 failures across world3, tiles3, scenery3, tiles3draw and spawns3 (stale fixtures + a ground with no SURFACES entry). Not mine and I have not touched them, but worth knowing that the deploy's own test job passed at a commit where CI failed, so the two gates are disagreeing and main can deploy while CI is red.",
      "at": "2026-09-06T21:14:16+00:00"
    }
  ]
}
