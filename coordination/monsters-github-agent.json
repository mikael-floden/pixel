{
  "domain": "monsters-github-agent",
  "updated_at": "2026-09-25T14:34:00+00:00",
  "health": "idle",
  "current": "idle — holding no files. Unit closed: his 2026-09-25T13:58Z redos on monsters/scree_lizard#idle#north-east and #north-west. Shipped a677b7a8e.",
  "progress": {
    "unit": "scree_lizard idle north-east (+ north-west mirror) — done",
    "verdicts_taken": 2,
    "verdicts_cleared": 2,
    "dangling_verdicts": 0,
    "scree_lizard_idle": "8/8"
  },
  "budget_remaining": "PixelLab USD $93.73 after the run (one v3 direction, ~$0.013)",
  "notes": [
    "[2026-09-25T14:15:00+00:00] NEW SITTING, new unit — the three notes above are the previous one's closed account (plume_brawler), kept. Read PROTOCOL.md, live/docs/review-contract.md, monsters/README.md, live/README.md and both partner boards. NOBODY HAS CLAIMED THESE TWO VERDICTS. monsters board (09-24T22:29, health 'running' but 15 h stale — PROTOCOL calls that down) claims the idle sweep over 32 candidates and names monsters/candidates/<those 32>/**, which covers scree_lizard; that claim is SPENT, not in flight: both monsters-animate.yml runs it launched finished at ~00:57 and ~01:00 today (36067707800 success, 36069691465 failure) and no animate run is queued or running — which is exactly why the state came out 6/8 and why he reviewed it at 13:58. monsters-assistant board is idle (09-22) and holds no files. CORRECTION TO MY OWN PREVIOUS NOTE (2): scree_lizard's north-east/north-west are NOT refill cases. `verdicts.py check` calls a missing facing a hole, but the feedback file says redo (art: null), not approved, and candidate.json has no north-east record and no north-east strip — that direction was never generated, so there is nothing in the candidate folder or in git history to put back. It is a redo, and a redo is a generation. swamp_ape and starfish_walker are the same shape and are left untouched.",
    "[2026-09-25T14:15:00+00:00] NOT TAKEN, deliberately: the four other open idle redos in the same file — starfish_walker#idle#north-east/north-west (13:55) and swamp_ape#idle#north-east/north-west (13:58). Not this unit, and `animate.py review --state idle` has no --only, so it would sweep all three monsters at once. Scoping with `idle --only scree_lizard` instead and leaving those four for the domain agent or the next sitting.",
    "[2026-09-25T14:34:00+00:00] TOOK: monsters/scree_lizard#idle#north-east and monsters/scree_lizard#idle#north-west, both redo, both written 2026-09-25T13:58Z, both with art: null. Unclaimed everywhere (the reasoning is the note above). The six approvals on the same state were left alone — an approval is never cleared (review-contract law 2).",
    "[2026-09-25T14:34:00+00:00] DID: `animate.py idle --only scree_lizard` — the domain's own resumable redo sweep, scoped to the one monster because `review --state idle` has no --only and would have swept three. --dry-run first: 1 direction, north-east, exactly as expected (north-west is its mirror and is never generated). One v3 roll, rung 0, 4 frames, job edf19c69: PASS, step 0.0845, drift 0.64, loop 0.0, no pad, no overflow. Eyeballed the landed strip rather than trusting the metric (README: no metric covers baked text) — the back-right view holds across all five frames, the breathing is the subtle kind he asks for, no glyph, no ground shadow. scree_lizard idle is now 8/8 and candidates/index.json was rebuilt by the run. CHECKS on the rebased tree: `verdicts.py check` = 0 dangling, exit 0. No pack.py — that layer is for graduated monsters under monsters/<id>/packed, and this is still a candidate. Credits $93.73 left. SHIPPED a677b7a8e.",
    "[2026-09-25T14:34:00+00:00] CLEARED: monsters/scree_lizard#idle#north-east and monsters/scree_lizard#idle#north-west, by clear_verdict the moment the direction landed — the only two entries this unit removed on purpose.",
    "[2026-09-25T14:34:00+00:00] A TRAP WORTH THE NEXT AGENT'S TIME, and it cost me a restore: the `prune-feedback` that runs automatically at the end of EVERY generation also deleted monsters/swamp_ape#idle#north-east and #north-west, which I had not claimed and had not acted on. The rule is `man and not isdir(anim_dir(...)) -> 'the art it judged is gone entirely (a discarded attempt)'`, and a direction that NEVER LANDED looks exactly like a discarded one: no record in candidate.json, no folder on disk. So his redo on a missing facing DELETES ITSELF the next time anybody, in any domain session, generates anything — silently, and the wiki will never show him that facing again, which is the one unrecoverable state. Restored both entries verbatim from origin/main in the same commit (status redo, art null, his own timestamps; no note existed on either). NOT FIXED IN THE PIPELINE: distinguishing 'never generated' from 'discarded' is a change to the shared prune path and nobody asked me for it — it is the monsters agent's call. The distinguishing fact is already on disk: a discarded attempt leaves nothing, but a never-landed direction is one whose SLOT has other directions with records and this one has none.",
    "[2026-09-25T14:34:00+00:00] DID NOT DO: (1) starfish_walker#idle#north-east/#north-west (redo, 13:55) — a DIFFERENT case from mine despite looking identical in the diff: those two carry real art hashes and the art is on disk, so they are a genuine 'roll it again', not a hole. Unclaimed, left. (2) swamp_ape#idle#north-east/#north-west (redo, 13:58) — the same never-landed shape as scree_lizard's and it needs one v3 roll (~$0.013); `verdicts.py check` still names both as HOLES. Unclaimed, restored, left. Both are one unit each for whoever sits next; neither is a refill.py case, there is nothing in the candidate folder or git history to put back. (3) The domain-wide packed-strip backlog the previous sitting on this board reported — still the monsters agent's, still not this unit."
  ],
  "requests": [
    {
      "to": "monsters",
      "text": "GRADUATION DOES NOT BUILD THE PACKED LAYER, and the game draws packed strips rather than raw ones. plume_brawler graduated on his approval today (1dc40efa1, monsters-graduate.yml) and arrived with no packed/ directory at all; I packed that one monster (a995e4326, 40 strips, zero generations). The domain-wide number after mine: `pack.py --check` reports 1566 strips stale over 34 monsters, and 38 of the 100 monster folders have no packed/ at all — every one the same cause, a sync or a graduation that built raw strips with nothing running pack.py afterwards. Two things, both yours: sweep the backlog when you are off the candidate idle sweep (pure CPU, no PixelLab), and add `pack.py --only <id>` to monsters-graduate.yml after its sync so a monster he finishes never reaches the game unpacked again. ALSO: the 09-24 idle sweep left THREE candidates at 6/8 on idle — north-east never landed, so its north-west mirror is missing too — and he pressed redo on all six facings today. scree_lizard is mine (claimed above). starfish_walker and swamp_ape are open and unclaimed. They are redos, not holes: no record, no strip, nothing for refill.py to restore, so they need one v3 roll each (~$0.013) rather than a refill.",
      "at": "2026-09-25T14:15:00+00:00"
    }
  ]
}
