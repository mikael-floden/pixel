{
  "domain": "scenery-github-agent",
  "updated_at": "2026-09-26T11:32:48+00:00",
  "health": "idle",
  "current": "idle — the 7 anchor rejections shipped in a8c39e956d and all 7 verdicts cleared. Every file released, including maps2/worlds3/the_game/world.json, which was never touched (heal found 0 dangling placements). No file in scenery/ in flight.",
  "progress": {
    "verdicts_taken": 7,
    "verdicts_cleared": 7,
    "generations_spent": 0,
    "states_deleted": 7,
    "anchors_promoted": 7
  },
  "budget_remaining": 0.0,
  "notes": [
    "[2026-09-26T11:32:48+00:00] WHAT I TOOK, AND HOW I FOUND IT — 7 verdicts, all anchor-state rejections. THERE WAS NO DIFF TO TRUST: this checkout is a single squashed import commit, so `git log` on live/feedback/objects.json has one revision and every file's commit time reads 2026-09-26T08:50Z. That matters beyond diffing — prune.py's fallback staleness test is `art committed BEFORE the verdict`, and in a checkout like this it reads every verdict as spent, so `prune.py --dry-run` returning 0 proves nothing here. I derived the unit from the DISK with the domain's other tool instead: promote_anchor.py --dry-run selected 7, 0 to hand back, and its survey() uses no commit times at all (verdict + hash + his own piece-level approval), so the squashed history cannot fool it in the destructive direction. The 7 were the only `rejected` entries in the file (21 redo, 21 withdrawn, 5138 approved). Each is the ANCHOR — its manifest state's sprite IS the piece's sprite.webp — which is why three earlier sittings walked past them: prune.py skips an anchor on purpose. NEWEST ENTRY IN THE FILE IS 09-19T10:51, so nothing this push carried was newer than my own board's last claim; what was outstanding was this stalled class, standing since 2026-09-18. UNCLAIMED when taken: coordination/scenery.json idle 09-18T23:30 'no file in scenery/ in flight', scenery-assistant.json idle 09-21 (its open unit is a wiki-side stamp question, no scenery file), this board idle 09-19; maps2 and maps2-assistant both IDLE with every file released, which is what let me reserve world.json for the agreed heal flow.",
    "[2026-09-26T11:32:48+00:00] WHAT I DID — `git sparse-checkout add maps2 games2` FIRST (my predecessor's paid-for trap: viewer_build publishes `placed` by joining maps2's worlds and is never fatal when they are absent, so it silently strips the field from 224 pieces), `pip install -r requirements.txt`, then `python3 pipeline/promote_anchor.py` — NOT prune.py and not by hand. prune.py skips an anchor by design; promote_anchor is the tool written for this exact dead end, and it promotes the best-rated approved sibling IN THE SAME LIGHTING FAMILY (so the piece's face does not change from lit to unlit), deletes the rejected art and its store object, retires the slot so state_variants does not read it as a gap and regenerate what he rejected, and clears the verdict in the same run. SHIPPED a8c39e956d (claim 9a2c898f32 pushed first). Gates on the final tree: finish_clips.py --check PASS (0 unfinished, 0 unreadable, anim_review + light_frames + pack green), retired.py --check PASS, heal_worlds.py --check PASS (0 dangling of 1486), light.py --check PASS. Checked the predecessor's trap explicitly rather than trusting a gate: `placed` is on 224 pieces after the rebuild, the same 224 as before. origin/main had not moved at the rebase, so no re-check was owed.",
    "[2026-09-26T11:32:48+00:00] THE TRAP I PAID FOR, AND IT IS IN THE TOOL — promote_anchor.py rmtree'd the PROMOTED state's whole directory while its own manifest entry went on naming that directory's CLIP. All 7 winners are trees/snags with a 5-frame `wind` clip, so the run left 7 clips pointing at frames and strips that were gone: games2 parseAnims drops a clip it cannot resolve and the wiki draws the still instead — the same silent stop as the hearths on 2026-09-15. finish_clips.py --check caught it BEFORE the push (14 findings: 'strip points at a missing file', 'playable verdict but no south strip'), which is exactly why the README says that gate runs before a push and not after. REPAIR: restored each winner's animations/ subtree from git (identical bytes, so no packed churn and no new hashed names), then removed the REJECTED anchor's own clip at the piece root, which is what nothing names once the state is popped. FIX so the class cannot recur: only the still moves now (the winner's own sprite.webp is removed, its clip stays where the manifest points), and _drop_dead_root_clip deletes the root clip only after proving no surviving state names any file in it — the 288-live-strips lesson. THE GENERAL SHAPE, for whoever next moves art between slots in this domain: A STATE IS NOT ITS STILL. Moving the still is one copyfile; the clip, its per-facing frames and its strips are the rest of the state, and the manifest is the only thing that knows where they are.",
    "[2026-09-26T11:32:48+00:00] WHAT I DID NOT DO, AND WHY. (1) streetlights/streetlight_034#lit_2#south (redo, 09-19T10:51, the newest entry in the file) — CLAIMED on this board on 09-19 and blocked there on zero budget, so it is consumed and stays standing; it needs a PixelLab generation and both pools still read 0. (2) The other 20 `redo` entries (hearth_900 x4, cupboard_010 x6, streetlight_034 lit_1 x2, lantern_post_026 x3, lantern_stand_018, washing_line_016, streetlight_024, flower_stand_015 x2) — every one asks for another take of a CLIP, which is generation the budget cannot pay for, and they belong to the scenery agent's animation passes (its 09-18 account and the assistant's 09-15 quote of the two sweeps to him are both about exactly these). Left standing, uncleared. (3) The 21 entries whose `status` he has WITHDRAWN (art + updated_at, no status) are not verdicts — acting on one would invent one. (4) No sweep of the other 5,159 approvals; an approval is never cleared. (5) NO PIXELLAB GENERATION AT ALL, so no budget was spent and no floor approached — the store calls were DELETEs. The generation loop stays the scenery agent's ('--plan OR NOTHING'). (6) Did not run wiki/tools/check-dangling.mjs (wiki/ is outside this sparse checkout); ghosts.sweep is the domain's own equivalent and reported 7 cleared, none left. (7) Did not hand-edit wiki/site/data.json — it rebakes from the tree in the deploy image and is the wiki agent's file. (8) Did not touch maps2/worlds3/the_game/world.json even though my claim reserved it: heal_worlds --check reports 0 dangling of 1486 placements, so no placement was drawn in any of the 7 deleted states and there was nothing for maps2's heal to re-pick.",
    "[2026-09-26T11:32:48+00:00] WHAT I CLEARED — exactly the 7 keys I took, by ghosts.sweep inside the promote_anchor run, in the same unit as the delete: owl_snag_001#not_lit_1#south, tree_063#not_lit_1#south, tree_065#not_lit_1#south, tree_067#not_lit_1#south, tree_072#lit_1#south, tree_075#not_lit_1#south, tree_086#lit_1#south (5187 -> 5180 entries; 0 rejected entries left in the file). NOTHING APPROVED WAS CLEARED — the 5138 approvals are untouched, and each promoted winner keeps its own (tree_086 lit_4 approved 5 stars, owl_snag_001 not_lit_10 approved 5 stars, ...), which is the whole point of promoting it. After the unit no key in live/feedback/objects.json names a path, state or direction that is not on disk: checked every manifest path of the 7 pieces resolves (0 dangling) and the promoted states keep their keys, so their verdicts still judge live art.",
    "[2026-09-26T11:24:55+00:00] CLAIM — 7 anchor rejections, all stamped 2026-09-18 (05:55-23:31 UTC), none claimed on any of the three scenery boards (scenery idle 09-18T23:30 'no file in scenery/ in flight'; scenery-assistant idle 09-21; this board idle 09-19). WHY THEY ARE STILL HERE: a rejection whose state's sprite IS the piece's own sprite.webp is the ANCHOR, and prune.py skips it on purpose ('deleting the anchor would strip the art every card shows'), so the chip stays red forever - the dead end promote_anchor.py was written for. Derived from the disk with the domain's own tools, not from a count: prune.py --dry-run selects 0, promote_anchor.py --dry-run selects exactly these 7 (0 to hand back). NOT TAKEN: streetlights/streetlight_034#lit_2#south (redo, 09-19T10:51) - claimed on THIS board 09-19 and blocked on zero budget, so it is consumed and stays standing; the other 20 redo entries need PixelLab generation against budget 0.0 and belong to the domain agent's animation passes.",
    "[2026-09-19T10:53:17+00:00] CLAIM — one verdict, the only entry that moved in f9c8d32bd: scenery/streetlights/streetlight_034#lit_2#south, approved -> redo (rating 5, art f658b2ff564059c4, no note). A facet key with a DIRECTION is an animation verdict in this domain (redo_facing_anim.py --from-feedback reads exactly <piece>#<state>#<dir> redo entries); redo means KEEP the state and generate another take of that facing's clip. UNCLAIMED when taken: coordination/scenery.json idle 2026-09-18T23:30 'no file in scenery/ in flight', coordination/scenery-assistant.json idle since 09-15, this board idle since 09-18T23:34 — no board names the piece or any of its files.",
    "[2026-09-18T23:34:00+00:00] WHAT I TOOK — two verdicts, both bare facet rejections on owl_snags/owl_snag_001: `#lit_3#south` (the only entry that moved in d5622105b — approved/rating 5/art 43c39bc5b2eb3110 -> rejected, art e23aad1b4da6bbc7, 23:19:16Z; diffed the file against its previous revision myself rather than trusting the count) and `#lit_1#south` (rejected 20:11:23Z, art f9148bf390399098). LIT_1 is the one worth reading: it landed ELEVEN MINUTES AFTER my predecessor's prune shipped (bf45beac8, 20:01), so no run had ever seen it — its art was still on disk with a live verdict pointing at it, the dangling row live/docs/review-contract.md exists to prevent. I derived it from the filesystem via the domain's own tool (`prune.py --dry-run` selected both), not from the diff. UNCLAIMED when I took them: coordination/scenery.json idle at 19:59 'no file in scenery/ in flight', coordination/scenery-assistant.json idle since 09-15; neither board named the piece or any file. Claims pushed BEFORE any work as 5d21b5529 (LIT_3) and c0d755e28 (LIT_1 added once the dry run found it).",
    "[2026-09-18T23:34:00+00:00] WHAT I DID — `git sparse-checkout add maps2 games2` FIRST (my predecessor's paid-for trap, one push earlier), `pip install -r requirements.txt`, `python3 pipeline/prune.py --dry-run` (2 states, exactly the two above), then `python3 pipeline/prune.py`. Not by hand and not redo_rejected.py: prune is the cutting-down-phase tool and the difference is the RECORD — a state merely deleted reads to state_variants as a gap and the next run regenerates what he just rejected, at his expense. It deleted both states from the PixelLab store and the repo (each a sprite + a 5-frame `wind` clip + its strip), retired them, cleared both verdicts in the SAME run via ghosts.sweep, republished retired.json (1209 pieces / 1144 states) and viewer_data.json. heal_worlds ran the agreed flow (claimed world.json on the scenery board as a9a6a3620, released it in my commit): owl_snag_001 is placed in the_game as NOT_LIT states only and as LIT_1/LIT_3 nowhere, so 0 re-picked, 0 dropped, 1486 placements untouched. SHIPPED aa0531738. Gates on the final tree: retired.py --check PASS, heal_worlds.py --check PASS (0 dangling of 1486), pack.py --check up to date (224 placed pieces), light.py --check PASS. Checked the trap explicitly rather than trusting the gates that missed it last time: `placed` is on 224 pieces after the rebuild, the same 224 as before. origin/main moved under the rebase by two coordination/ commits only (a monsters claim and my own heal claim), nothing under scenery/, so no re-check was owed.",
    "[2026-09-18T23:34:00+00:00] WHAT I DID NOT DO, AND WHY. (1) `#lit_2#south` is left alone: he WITHDREW its status at 22:51:09Z — the entry now carries `art` and `updated_at` and no `status` at all — so it is no longer a verdict, and prune does not select it. Acting on it would be inventing one. (2) No replacement art for either state. A bare rejection is DELETE-AND-RECORD by his 2026-08-17 law ('when I reject from here on - that doesn't mean something new should be generated ... what's left is what we will stay with'); neither entry carried a note, and neither is `redo`, which is the key that carries the regenerate sense. owl_snag_001 keeps LIT_2, LIT_4 and its ten NOT_LIT states. (3) No PixelLab GENERATION, so no budget was spent and no floor approached; the generation loop stays the scenery agent's either way ('--plan OR NOTHING'). (4) No sweep of the other 5,187 entries: one review landed, two verdicts were actionable against the disk, and taking an older one here would consume it off the domain agent's board. (5) Did not run wiki/tools/check-dangling.mjs (wiki/ is outside this checkout); ghosts.sweep is the domain's own equivalent and reported 2 cleared, 0 dead chips left. (6) Did not hand-edit wiki/site/data.json — it rebakes from the tree in the deploy image and is the wiki agent's file.",
    "[2026-09-18T23:34:00+00:00] WHAT I CLEARED — exactly two keys, by ghosts.sweep inside the prune run, in the same unit as the delete: `scenery/owl_snags/owl_snag_001#lit_3#south` and `scenery/owl_snags/owl_snag_001#lit_1#south` (5,189 -> 5,187 entries). Nothing approved was cleared anywhere, per the review contract — the piece-level approval and the ten NOT_LIT approvals are untouched, and note that `#not_lit_1#south` / `#not_lit_3#south` are DIFFERENT, live states that only match a naive 'lit_1'/'lit_3' substring search. After the unit, no key in live/feedback/objects.json names a path, state or direction that is not on disk.",
    "[2026-09-18T20:12:00+00:00] WHAT I TOOK — one verdict, the only thing that moved in c15246bbc: `scenery/owl_snags/owl_snag_002#lit_3#south`, flipped from approved (2026-08-15, rating 5, art 9e90e07fea2c2a91) to `rejected`, bare, stamped art 1f95bed50b6e15b6. Diffed the file myself against its previous revision rather than trusting a count; that single entry is the whole push (5190 -> 5189 entries after clearing). A facet `rejected` written after 2026-09-03 04:40 means DELETE THAT STATE (live/README.md; the button is labelled '✕ remove' and `redo` now carries the regenerate sense), and scenery/README.md's 'a rejection is a standing removal order' is the domain half. UNCLAIMED when I took it: coordination/scenery.json was idle at 19:11 with 'no file in scenery/ in flight' (37 minutes BEFORE the review landed at 19:48, so it cannot have seen it) and coordination/scenery-assistant.json idle since 09-15. Neither board named the piece or the file. Claim pushed as 9e55dddd9 BEFORE any work.",
    "[2026-09-18T20:12:00+00:00] WHAT I DID — `python3 pipeline/prune.py --dry-run` (1 state, exactly the one in the diff), then `python3 pipeline/prune.py`. NOT by hand and not redo_rejected.py: prune is the cutting-down-phase tool, and the difference is the RECORD — a state merely deleted reads to state_variants as a gap and the next scheduled run regenerates what he just rejected, at his expense. The run deleted the art (lit_3 sprite + the 5-frame `wind` clip + its strip) from the PixelLab store and the repo, retired the state, cleared the verdict in the SAME run via ghosts.sweep, and republished retired.json (1209 pieces / 1142 states) + viewer_data.json. heal_worlds ran its agreed flow (claimed world.json on the scenery board as acc93c617, released it in my commit): owl_snag_002 is placed in the_game as NOT_LIT_10 and as LIT_3 nowhere, so 0 re-picked, 0 dropped, 1486 placements untouched. SHIPPED bf45beac8. Gates on the final tree: retired.py --check PASS, heal_worlds.py --check PASS (0 dangling of 1486), pack.py --check up to date (224 placed pieces).",
    "[2026-09-18T20:12:00+00:00] THE TRAP I PAID FOR, AND THE NEXT STAND-IN SHOULD READ IT FIRST: A GITHUB AGENT'S CHECKOUT IS SPARSE — scenery, coordination, live and the root, with maps2/ and games2/ ABSENT. Every scenery pipeline script ends in viewer_build.build(), which publishes `placed` from pack.placed_states() by joining our states against maps2/worlds3/*/world.json, and which is deliberately NEVER FATAL when the worlds are missing. So my first prune run silently stripped `placed` from all 224 pieces that carry it (grep '\"placed\"': 224 -> 0) — the field the wiki shows him as 'in the world', published on 09-15 precisely so he stops reviewing states the game never draws. Worse, the two gates that should have caught it both PASSED on the absent world: heal_worlds.py --check said 'every published world resolves' over zero worlds and pack.py --check said 'no pieces selected'. `git sparse-checkout add maps2 games2`, rebuild, and the diff came back to the LIT_3 removal alone (-59 lines, the same shape as the tree_024 prune one commit earlier). WIDEN THE CHECKOUT BEFORE RUNNING ANYTHING THAT ENDS IN viewer_build — and read 'no pieces selected' from pack --check as a missing tree, never as a clean tree.",
    "[2026-09-18T20:12:00+00:00] WHAT I DID NOT DO, AND WHY. (1) Nothing else in live/feedback/objects.json: 5,189 entries remain and none of them is a verdict this push made actionable — one entry moved and I acted on it. I did not sweep the domain for older unactioned verdicts; that is the domain agent's pass, not this unit, and taking one here would consume it off their board. (2) No replacement art for LIT_3. prune is DELETE-AND-RECORD by his own 2026-08-17 law ('when I reject from here on - that doesn't mean something new should be generated ... what's left is what we will stay with'), and the entry carried no note — nothing matching redo/regenerat/should-be — so it is a bare rejection, his fast path, and generating would have reverted his decision. owl_snag_002 keeps LIT_2, LIT_4 and its ten NOT_LIT states. (3) No PixelLab generation at all, so no budget was spent and no floor was approached; the scenery board reports budget_remaining 0.0 and the generation loop stays the scenery agent's either way ('--plan OR NOTHING'). (4) Did not run wiki/tools/check-dangling.mjs — wiki/ is outside the sparse checkout and ghosts.sweep is the domain's own equivalent; it reported 1 ghost cleared and none left. (5) Did not hand-edit wiki/site/data.json; it rebakes from the tree in the deploy image, and it is the wiki agent's file.",
    "[2026-09-18T20:12:00+00:00] WHAT I CLEARED — exactly one key, by ghosts.sweep inside the prune run, in the same unit as the delete: `scenery/owl_snags/owl_snag_002#lit_3#south`. No entity-level entry and no other facet on that state existed to clear (the surviving `scenery/owl_snags/owl_snag_002#not_lit_3#south` is a DIFFERENT, live state — it only matches a naive 'lit_3' substring search; NOT_LIT_3 is on disk). Per the review contract his approvals were left untouched: nothing approved was cleared anywhere. After the unit, no key in the file names a path, state or direction that is not on disk."
  ],
  "requests": []
}
