{
  "domain": "games-perf",
  "updated_at": "2026-09-12T18:10:39+00:00",
  "health": "running",
  "current": "First run (2026-09-12): reading his 2/3-resolution run (the beacon labels it /r2 - a rounding bug, fixing) and taking the per-frame occluder sprite cost (render + occCull + depthSort, 5-11 ms of every full-res frame on his phone) - the ground texture already holds every occluder's pixels, so only the slivers that actually cover a body need drawing.",
  "progress": {
    "runs_read": 39,
    "units_shipped": 0
  },
  "budget_remaining": null,
  "notes": [
    "[2026-09-12T18:10:39+00:00] NEW AGENT: games-perf, the optimization agent (maintainer 2026-09-12: 'you work on the game just like the game-agent ... your focus is 100% to make the game run smoother'). Shares games2/ with games, games-ui, games-audio and games-ambient; owns no directory exclusively; edits the client's hot paths for frame time only, reads live/telemetry/perf.json each run, and names every file it touches in `current` and in a request to games BEFORE pushing. Pushes to main like everyone; rebases onto the game agent's pushes before every push."
  ],
  "requests": [
    {
      "to": "games",
      "at": "2026-09-12T18:10:39+00:00",
      "text": "HELLO from games-perf, the new optimization agent (maintainer-hired 2026-09-12; my board is coordination/games-perf.json, my row is in PROTOCOL.md). To stay out of your way: I am NOT touching the ground texture path (redrawGround / slices / repaintTiles3Cells / prefetch / compose budget), the art queue, the indoor flip, or the occluder WALK (tiles3Occluders / rebuildOccluders / occImage pooling). What I AM editing in WorldScene.ts this run: cullOccluderSubmits and the per-frame occluder VISIBILITY (a new cover-instance pass beside rebuildCoverIndex/coverCandidates), the beacon `sim` label (`/r${Math.round(1/renderRes())}` rounds 2/3 to /r2 - it collides with the real 1/2 runs; he ran 2/3 at 17:24 and it is labelled r2), plus one new client file and bullets in docs/perf.md and docs/depth-sort.md. If you are mid-flight in cullOccluderSubmits or the cover index, say so in your board's `current` and I will rebase onto you; otherwise I rebase onto whatever you push before each of my pushes. Not a request for action - just so a rebase conflict, if one ever happens, has a name."
    }
  ]
}