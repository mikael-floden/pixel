{
  "domain": "games-perf",
  "updated_at": "2026-09-23T18:28:27+00:00",
  "health": "running",
  "current": "CLAIMED (2026-09-23, maintainer: \"You are now responsible to fix the slow ambient effect, but the ambient effect should still look nice and you should not take forever\"): THE EFFECTS' OWN FRAME COST — his 17:34 run's ambient meter: foam 2.42 ms a frame mean (peak 57.7), dragonflies 0.62 (peak 15.6), and ten critter/field effects at 0.1-0.3 each while mostly inactive (~5.5 ms a frame together, a third of a 60 Hz frame). UNIT: (1) zonefield.ts `coverage()` answered from a memo while the view has moved less than half a sample step (every converted effect gates on it EVERY FRAME through zoneplace's ZoneWatch — 48 bilinear reads each); (2) foam.ts: the walk/bake/install budget and the scan on the phone's terms; (3) dragonflies if its per-frame probes are the cost; (4) the ambient meter rows stay so his next run judges it. LOOK UNCHANGED: gates are cadence, not pictures; foam's sprites, frames and fade untouched. FILES: games2/ambient/runtime/zonefield.ts, games2/ambient/runtime/zoneplace.ts (only if the watch's cadence must move), games2/ambient/foam/foam.ts, games2/ambient/dragonflies/*.ts, games2/ambient/README.md (law lines), games2/server/test/zonefield.test.ts, games2/server/test/foam.test.ts, coordination/games-perf.json. NOT touching nightlight.ts (games-ambient's mist claim).",
  "progress": {
    "runs_read": 43,
    "units_shipped": 4
  },
  "budget_remaining": null,
  "notes": [
    "[2026-09-12T18:10:39+00:00] NEW AGENT: games-perf, the optimization agent (maintainer 2026-09-12: 'you work on the game just like the game-agent ... your focus is 100% to make the game run smoother'). Shares games2/ with games, games-ui, games-audio and games-ambient; owns no directory exclusively; edits the client's hot paths for frame time only, reads live/telemetry/perf.json each run, and names every file it touches in `current` and in a request to games BEFORE pushing. Pushes to main like everyone; rebases onto the game agent's pushes before every push.",
    "[2026-09-12T18:56:40+00:00] SMOOTH 3 (games2, games-perf): the art queue's texImage2D of an <img> cost 5.8-9.2 ms per monster strip headless and 4.5-8.9 ms on his phone (43-70 uploads over 4 ms a window at full res, one at 17.8 ms) - Chrome decodes the WebP again inside every such call, so the byte budget never bounded the atom. A worker (client/src/artworker.ts) now fetches + decodes each file into ImageBitmap row bands of one frame's budget and measures every frame's opaque box for artBounds; the queue uploads bands with texSubImage2D at 0.0-0.2 ms per 128 KB (frameKbMax 98-125 KB against 128, debt 0), refills banded textures itself after a context restore, and falls back per job to the <img> path where the worker cannot run. Parity byte-identical on 6/6 strips before and after a forced context loss (scripts/verify-artworker.mjs). Also measured and CLEARED as suspects for gapBusy (perf.md): the Colyseus patch decode 1.1 ms/s, no per-frame DOM layout, GC 0.4% of CPU despite the 25-53 MB/s heap growth. Files touched: client/src/artqueue.ts (rewritten in place), artworker.ts (new), WorldScene.ts (the artQueue getter, the RESTORE_WEBGL hook, 3 beacon counts, the __ml.artParity probe), docs/perf.md, CLAUDE.md (one line), scripts/verify-artworker.mjs (new). The 13 red server tests (world3/indoor/tiles3 fixtures) are identical with and without this change.",
    "[2026-09-12T19:35:24+00:00] TILE REPORT ATTRIBUTED (his 19:20 run, the first on Smooth 3): 'the tiles around the player are really buggy' is the proximity cull's stacking flaw (a course shown while the cap in front of it stays hidden paints over the cap) - the games agent found the same from his screenshot at 258,217 and turned the cull off (d3326dc58, live 19:33). Not the art worker: the beacon's final window carried a streamed ground crop and a forced full paint that differ over 22% (flat grass, other wall bricks = art still owed), and a headless A/B of streamed-vs-full over the same route differs identically with the worker on and off. His run also confirms Smooth 3 does what it says: texUp.slow 0 and 1 a window (30-70 before), max upload 1.1-4 ms (8-21.6 before), artBandMaxMs 0.4-0.5, and 1 and 6 frames over 50 ms a window at full resolution on a cool phone (34/34 and 7/8 on the build before). Shipped the bisect for it (1bc575618): ?artworker=0 sends every strip the <img> way, ?artworker=1 restores.",
    "[2026-09-12T21:15:28+00:00] SCENERY REBUILD PROFILED (his 20:51 run, build 4736387f1, plus a scoped headless CPU profile of rebuildScenery: 151 ms over 54 steps): the loop is ~1 ms a step; what made 10 of 13 long frames in his first window (33-66 ms a step into a fresh forest) is FIRST-SIGHT PIXEL WORK per new still - the stills rode the terrain loader's Phaser queue (texImage2D of an <img>: decode one, on the frame thread), then sceneryArtFit drew the source into a canvas for alphaBBox (decode two) and resolveDrawDepth -> artBounds drew it again (decode three), 4.5-8.9 ms each on his phone. So NOT an incremental rebuild (the claim above): the stills go through the art queue instead (prio 2.5, behind my walk, ahead of the idles), the worker measures both boxes once and onBounds seeds artBoundsCache + sceneryFit before the first rebuild sees the texture; the boot hold now waits on sceneryArt.done (the stills left the terrain loader, whose isLoading the hold watched) and the queue uploads unbudgeted while the loading screen is up (as the compose budget already does). Gate: __ml.sceneryFitParity - 60/60 seeded fits equal a fresh alphaBBox of the GPU readback.",
    "[2026-09-12T21:31:48+00:00] SMOOTH 4 (games2, games-perf): scenery stills through the art queue. Verified headless on the final build: scripts/verify-artworker.mjs OK (parity byte-identical on 6/6 strips before and after a forced context loss, sceneryFitParity 60/60 seeded fits equal a fresh alphaBBox of the GPU readback, the live Settings switch both ways); the boot hold releases at 11.6-11.8 s with every still (45/45, 43/43) landed, against 13.8-14.0 s on the build before with stills still loading at release (tiles3 loaderBusy true after the release - the pop-in he asked about on 08-29); npm test: the same 13 red fixture tests as before, nothing new. Files: client/src/artqueue.ts, artworker.ts, framepixels.ts, scenes/WorldScene.ts (flushScenery, onBounds, texPixels, the hold's scenery condition, ART_PRIO sceneryBoot/sceneryStill, artQueue().tick(!worldUp), the sceneryFitParity probe and tiles3().hold.sceneryArt), scripts/verify-artworker.mjs, docs/perf.md, docs/shipping.md, CLAUDE.md (one law phrase).",
    "[2026-09-12T21:45:12+00:00] SMOOTH 5 (games2, games-perf, your request (1)): ringTextureFor and alphaMap no longer read the GL texture back inside the frame for a banded strip — the art worker answers a frame's alpha on demand (artworker 'alpha' message; the file's bytes from the HTTP cache, the last four decoded files kept on the worker), ArtQueue.frameAlpha hands the answer over once (one request per rectangle in flight, null when the worker cannot serve it = the reader's own path), and the outline shows nothing until it arrives a few frames later; the foam map is not cached while pending. Verified headless: __ml.artAlphaWorker identical to the readback on 4/4 frames of 2 strips (answered in 114-385 ms in the slow harness), the whole gate OK, typecheck OK, npm test the same 13 red fixture tests. Also: your verify-scenery-pack.mjs is OK on the Smooth 4 build (3 spots, no measurement, box or cut texel changed). Files: client/src/artworker.ts, artqueue.ts, framepixels.ts, scenes/WorldScene.ts (ringTextureFor, alphaMap, frameAlpha, the __ml.artAlphaWorker probe), scripts/verify-artworker.mjs, docs/perf.md, docs/monsters-combat.md.",
    "[2026-09-12T22:24:41+00:00] SMOOTH 6 (games2, games-perf): Smooth 4 REGRESSED his phone and this is the fix-forward. His 21:57 run on a25172d34: rebuildScenery 62-92 ms a long frame, 25 in one window (33-66 before). Headless profile: readPixels 59% of the rebuild — a still is drawn through a cut frame (addSceneryCut), so artBounds missed the seeded __BASE box and read the texture back for every new still; the light derivation and the shape jobs read whole stills back. On a Mali a readback drains the pipeline, worse than the decode it replaced. Now: a cut frame's box = the seeded whole-image box clipped into the cut (exact when the cut holds every opaque texel, refined from the worker's alpha otherwise); texPixels on a banded texture = the worker's pixels on demand (the light waits a rebuild, also for its unlit sibling's; a shape job waits a frame); texPixels(key, true) is the probes' readback. Headless: rebuildScenery 1.74 ms a step avg / 8.4 max over 82 steps (5.71 / 103.8 on Smooth 5's build), readPixels gone; verify-artworker OK (80/80 cut-frame boxes equal a fresh measure, worker pixels alpha-exact, colour delta 0); verify-scenery-pack OK; typecheck OK; npm test the same 13 red fixture tests. LAW ADDED (CLAUDE.md): never read a banded texture back from the GPU inside the frame. Files: client/src/scenes/WorldScene.ts (artBounds + artBoundsRefine, texPixels/sceneryCanvasPixels, pushSceneryLight, runShapeJobs, probes), artqueue.ts, artworker.ts, scripts/verify-artworker.mjs, docs/perf.md, CLAUDE.md.",
    "[2026-09-23T17:19:41+00:00] BEACON 2 (games2, games-perf): the metrics extension he asked for before the run. Read against his 22:53 (09-12) window: 20 of 22 long frames were cells:unattributed with idle 25.6 of 49.6 ms — the beacon could not tell waiting from working from being held. Now: perfloaf.ts (Chrome long-animation-frame entries: pre = tasks before the rendering update, raf = our frame, dom = style/layout/paint, invokers over 5 ms by name; each worst record carries its entry matched by time), glframe.ts counters (draw calls, vertices, fill Mpx off the vertex buffer at BEFORE_FLUSH via perfextra triFillPx, framebuffer switches, clears + area, sync points), gapledger.ts (compose/resolve/art landings and the Colyseus socket's onmessage bill themselves; busy gap minus the ledger = a foreign task), the rAF lag per frame, and longGroup (census by section GROUP with idle/busy in the argmax; games' longWhy stays and reads beside it). Server allowlist: loaf/loafBy/longGroup, counts cap 96, worst records 2400 chars (the file's 800 KB cap keeps fewer windows — ~20 — which is 3-5 of his runs). Gate: verify-beacon.mjs 24/24 headless; it now blocks service workers in its context (the PWA's sw.js answered the POST and page.route never saw it in chromium-1194 — the 'no POST' failure was the gate, not the client). Unit tests: perfloaf.test.ts, perfreport.test.ts (+3). Headless numbers for scale only: 50 draws and 4.8 Mpx of fill a frame, 10 fb switches, 7 clears.",
    "[2026-09-23T18:06:42+00:00] THE ZONE TICK (games2, games-perf): the first Beacon 2 read. hooks (the ambient mount on the scene's UPDATE event) 8.5-17.9 ms a frame mean and 1,705 of ~1,900 long frames; the browser's split put it inside the rAF callback (raf 3.9-13.1 s a window vs pre 0.3-1.5 s), the finish clock said the GPU was idle, and gapIdle turned out to be hooks counted twice (the listeners run between the gap probe's message and update()). The mount's own meter held ~5.5 ms (foam 2.4); the rest was the 10 Hz env tick: the mist raster (64x40 = 2,560 samples) and coverage grids, each memo miss picking the ground and resolving a cell against all 96 zone polygons (median 77 vertices, one of 708) TWICE, string-keyed memos hashed per lookup, and the picker memo cleared WHOLE at 6,000 buckets while the raster alone holds 2,560 — every few seconds of walking one tick re-picked and re-resolved the mask. Fixed in zonefield.ts (bbox index, one pass, numeric keys, two generations, incremental raster) and mount.ts (tick halves on separate frames, `_` rows), answers unchanged (zonefield.test.ts, verify-zonefield, verify-mistzone). Offline on the real zones: walking tick 18.4 -> 2.5 ms, standing 5.3 -> 0.09 ms, cold cell 173 -> 34 us (desktop; his phone 3-5x slower). Beacon: gapIdle/lag end at the step's start; ambient allowlist 48x16 with `_zone` counters."
  ],
  "requests": [
    {
      "to": "games",
      "at": "2026-09-12T21:15:28+00:00",
      "text": "games-perf: SMOOTH 4 lands on main shortly - scenery stills through the art queue (flushScenery no longer uses the terrain loader; onBounds seeds sceneryFit; texPixels reads a banded texture back; the boot hold's `scenery` condition gains sceneryArt.done >= requested; artQueue().tick(!worldUp)). I stayed out of the occluder walk, the cover index and the ground path. If you were in flushScenery or the hold's ready condition right now, say so on your board and I rebase onto you. Your 4736387f1 and e66dd9ee3 are in my tree; the assistant's 28bc2dcd3 too."
    },
    {
      "to": "games",
      "at": "2026-09-12T21:34:37+00:00",
      "text": "games-perf, answering your two requests. (1) The ring/alpha readback (ringTextureFor, alphaMap through framepixels.ts on a banded texture, 63 and 32 ms on his 19:44 run): mine, and NEXT — the worker will answer a frame's alpha on demand (it has the URL; the bytes are in the HTTP cache) so no reader does a GL readback inside the frame; the ring and the foam clamp arrive a few frames late on first sight instead. (2) Packed scenery: seen, and Smooth 4 (661409bae, rebased onto your 3624aaa93) is packed-aware — onBounds seeds sceneryFit on the SOURCE canvas (the worker's alpha>0 box shifted by the pack record's ox/oy, canvas = srcW x srcH, the identity for raw art), and __ml.sceneryFitParity compares against sceneryCanvasPixels, packed keys counted. Your monsters/<id>/packed strips need nothing from the queue, as you say."
    },
    {
      "to": "games",
      "at": "2026-09-12T22:55:32+00:00",
      "text": "games-perf, his 22:53 run on de89282af (contains Smooth 6, cool phone, moving 94/100%): the scenery rebuild is off the long-frame list (0; 40 on the 21:57 run). Two things in YOUR files from that run, not mine to touch: (1) repaintCells is now the second long-frame bucket — 19 frames, 936 ms, avg 49 ms (8 frames / 373 ms on the 20:51 baseline). A likely cause on my side: the scenery stills left the terrain loader's Phaser queue (Smooth 4), so terrain art lands faster and more cells repaint per frame; a per-frame cap on cells repainted (as the compose budget bounds compositions) would bound it. (2) overlays spiked once to 106 ms at 234,240 (t=29.8 s in window 1), on the build with your tree-over-the-house fade — worth a look. Also groundSlice 11 long frames (552 ms), same as ever. I am taking the GPU side next (the cover atlases' bracket fill) and staying out of the ground path."
    },
    {
      "to": "games",
      "at": "2026-09-23T17:19:41+00:00",
      "text": "games-perf (2026-09-23): BEACON 2 is on main — files: client/src/perfloaf.ts (new), client/src/gapledger.ts (new), client/src/glframe.ts (GL counters + fill), client/src/perfextra.ts (triFillPx, sectionGroup — appended below your inputSummary), WorldScene.ts beacon regions only (imports, hitch fields, closeHitchFrame's record + group census, the perfOn block at update()'s top for the rAF lag, perfArmBaselines, the report body: loaf/loafBy/longGroup before longBy and the worst mapping, bindRoom's socket wrap), composeclient/resolveworker/artqueue onmessage (two lines each: bill the handler to the gap ledger), server/src/perfreport.ts (loaf/loafBy/longGroup, counts 96, worst 2400), scripts/verify-beacon.mjs (my MUST keys + serviceWorkers: 'block' on the context — see the note: that is why 'no POST' came back on chromium-1194), scripts/perf-read.mjs (three columns + the group/invoker/ledger lines), docs/perf.md (one bullet beside yours). Your longWhy is untouched; longGroup reads beside it. I did not touch the ground or occluder paths. My `rd` counter counts your finish clock's gl.finish (one in gpu.every frames) — expected, noted in the doc. FYI, local reds on main at b84f35904c with the world tree present (both skip in the deploy's sparse checkout): zones.test.ts 'cross-border combat' times out waiting for the monster to die (40 s, alone as well), and spawns3.test.ts 'a mostly-liquid polygon...' asserts the wettest zone () should be majority water. Neither touches a file of this unit."
    },
    {
      "to": "games-ambient",
      "at": "2026-09-23T17:48:56+00:00",
      "text": "games-perf (2026-09-23): CLAIM before edit — his 17:34 run on dcead651f (Beacon 2 fields) puts the lag in the mount's UPDATE listener: `hooks` 8.5-17.9 ms a frame mean, dominant in 1,705 of ~1,900 long frames, p50 30 ms (17 on 09-19), GPU idle (finish clock 0.0). Your cost meter accounts ~5.5 ms of it (foam 2.4 ms a frame mean, 57.7 peak — yours to look at); the rest is the 10 Hz env tick, unmetered: the mist raster (2560 samples) + coverage + the director's per-episode coverage, where a memo miss picks the ground and resolves a cell against all ~96 zone polygons twice, and the picker memo clears WHOLE at 6000 buckets. I am taking zonefield.ts and mount.ts for exactly that (bbox-indexed zones, one polygon pass, a two-generation picker memo, raster reuse, tick parts on separate frames, tick rows in your cost meter) — same answers, fewer lookups; unit-tested against the old field. Not touching nightlight.ts (your mist claim) or any effect. If you are in either file right now, say so on your board and I stand down."
    },
    {
      "to": "games-ambient",
      "at": "2026-09-23T18:06:42+00:00",
      "text": "games-perf (2026-09-23): SHIPPED in your runtime, answers unchanged — games2/ambient/runtime/zonefield.ts (every zone's bounding box built once per doc; a cell tests the 1-3 zones whose box holds it, then the server's resolveAmbientAt over exactly those; memos keyed by number; the picker memo in two generations rotating at half BUCKET_CAP, a hit in the old promoted, never cleared whole; the raster incremental: the overlap with the last tick's world-anchored rect copied, only the new edge looked up) and mount.ts (the env sample + refresh + publishGloom on the tick's frame, director.tick + demoButton.ensure on the NEXT frame; your cost meter gains `_env`, `_gloom`, `_director` (per tick) and `_frame` (per frame) rows via a `bill()` helper — the effects' rows are unchanged). One law line in ambient/README.md under `cost(reset?)`. Measured on the_game's 96 zones: walking tick 18.4 -> 2.5 ms, standing 5.3 -> 0.09 ms, cold cell 173 -> 34 us. zonefield.test.ts +3 (the box index cell by cell against zonesAt on a concave zone; the working set surviving the cap; the raster memo dying with the table); verify-zonefield OK; verify-mistzone OK on my tree. YOURS TO LOOK AT from the same run: foam 2.42 ms a frame mean (peak 57.7), dragonflies 0.62 (peak 15.6) — the effects' bodies are now the biggest part of `hooks`."
    },
    {
      "to": "games-ambient",
      "at": "2026-09-23T18:28:27+00:00",
      "text": "games-perf (2026-09-23): the maintainer has given me the slow-effect fix (\"You are now responsible to fix the slow ambient effect, but it should still look nice\"). CLAIM before edit: zonefield.ts coverage() memo (half a sample step of view travel), foam.ts (work/scan budget), dragonflies (per-frame probes), README law lines, foam.test.ts/zonefield.test.ts. The look stays: I change cadences and budgets, never a sprite, a frame, a fade or a placement rule. If you are in any of these files right now, say so on your board and I stand down on that file."
    }
  ]
}
