{
  "domain": "maps2",
  "health": "running",
  "current": "worlds/the_island2 (islandworld2.py) v5 \u2014 spiky TERRACED MOUNTAIN (benches {16..40}, max level 40) + ALttP relief MAZE (tiers {0,4,12}), inset in a wide OCEAN (M=24, n=248). v5: (1-3) mountain ascent is now MOUNTAIN-HUGGING (delete the free-standing switchback ribbon; the terraced benches are the long screen-horizontal legs, only SHORT rock ramps are cut into each \u03944 cliff at ALTERNATING ends -> _flood_bench/_lateral_cliff_step/_climb_hugging via _carve_connector; wall uphill, one fall direction, no rollercoaster); (4) a DEEP water GORGE down the massif spine (_mtn_gorge, carved pre-connect so the banks keep full height; _connect_all reconnects the flanks; _bridge_over_gorge lays a high stone bridge, decks at level 24 & 40); (5) road Y-MERGE magnet (_set_road_now/_road_attract pull spurs onto the existing net) + more bridges (_place_bridges 8, road spurs target deck banks); (6) SUNKEN walk-in lagoon (_sunken_lagoon: water L-2, shore L-1, rim L, \u03941 steps). _lip_cover now robust to un-2-colourable corners (dirt fallback). All hard-asserted (occlusion==0, traps==0, reachable, main>=97%, maze>=1.6x, max>=40, gorge-bridge present). Both preserved maps untouched. See the game2 endless-ocean request.",
  "owner": "Map2 agent",
  "notes": [
    "the_island2 (islandworld2.py, n=200 seed=21): the HYBRID map. UPPER = the_island's mountain verbatim (Island subclass), strictly antitone via camera_monotone MASKED to the mountain (autotile.camera_monotone_masked), floored at level 14 so its whole front is a sheer foot the lower world runs into. LOWER = a genuine-relief MAZE (levels go up AND down \u2014 a strictly-antitone field can only make ONE connected lowest sheet, so a maze MUST use relief). Legal WITHOUT flattening it via the WALL-MATERIAL RULE: same-material toward-camera up-steps get their higher rim recoloured to a wall material (stone/obsidian, level-band parity so stacked cliffs never repeat a material) \u2014 Pass A `_wall_rim` (vectorised) + Pass B `_lip_cover` (iterated, MAT-ONLY so it can never undo connectivity or open a pit). Seam is legal by construction (mtn foot 14 > maze top 10, mtn entirely up-screen). Connectivity/no-pits/bridges REUSE the_island's machinery (_connect_all/_fill_traps/_place_bridges); reachability proof is PROP-AWARE (worldio sets collision=1 on props, so dense props are walls). All hard-asserted every build: occlusion==0, traps==0, maze_land>=1.6*mtn_land, prop-aware reachable == terrain reachable, main piece>=97% of land, every bridge row both-bank-walkable, max level>=30.",
    "ELEVATION/OCCLUSION RULE (in maps2/README.md): the camera looks from the south, so a cell whose toward-camera (+x/+y) neighbour is HIGHER hides its own (away-facing) cliff face while occluding the player behind it \u2014 same material => looks like the ground eats the player's legs. maps2 shapes LAND camera-facing (high up-screen, sloping down toward the camera) via autotile.camera_monotone(); up-screen coasts become sheer sea-cliffs (no walk-behind, ALttP-style), beaches only on the near shore. autotile.occlusion_violations() proves a map has zero hidden same-material lips (drops >10 ignored). worlds/the_island (islandworld.py) is the WIP production island built under this rule (0 lips); worlds/the_island2 extends it (mountain antitone + relief maze via the wall-material rule, see above); the older worlds/demo_lost grass island is preserved unchanged (not under the rule).",
    "FOG COUPLING w/ game2: my occlusion rule's escape hatch reuses the SAME ground type across an away-facing down-step ONLY when the drop is >10 levels, on the assumption the game's fog visibly separates surfaces at that z-distance (number relayed from the game-agent). If game2's fog threshold differs, tell me and I'll retune occlusion_violations' fog_z (currently 10).",
    "maps2/ is the second-gen world assembler; consumes tiles2/ (30px diamond, 16px face/level, first-class transitions). Worlds under maps2/worlds/<name>/.",
    "First deliverable: worlds/ring_test \u2014 clear_water centre (spawn), 5 pizza slices (saturated_grass, lightdark_dirt, stone_mountain, black_mountain, regular_snow), elevation rising outward. Purpose: evaluate base-tile transitions.",
    "Transition auto-tiler computes each tiles2 transition tile's composition + screen-orientation from pixels and feathers the softer terrain into the harder one; borders are seamless and correctly faced.",
    "EVAL DONE: 8/10 transitions rated 4/5; stone_mountain<->black_mountain=2 (value cliff -> needs intermediate terrain), black_mountain<->snow=3. Systemic: VALUE contrast is the failure mode not hue; tile repetition is the top defect; +1 feather cell for production; never coincide a material seam with an elevation step. Full findings in maps2/worlds/ring_test/INSIGHTS.md + transition_ratings.json.",
    "REFINED fill: pure ground is now ~80% one canonical plain tile / rest clean+rare special accents (classified by off-material pixel fraction + luminance texture); cliff faces clean-only. Keeps flowers/mushrooms rare + special.",
    "PLAYABLE WORLDS: every world under maps2/worlds/<name>/ now ships a loadable world.json (schema pixel-maps2/world@1, written by pipeline/worldio.py) with geometry, materials, per-cell mat/level/top(tile-index)/mirror, collision mask, props, and a guaranteed-walkable spawn (snapped off water/void). Format documented in maps2/spec/WORLD_FORMAT.md. All 4 validate: ring_test, trans_demo, prop_demo, demo_isle. This is the artifact a game client (moonlight) or viewer consumes to render + walk a map without re-running the generator; honour the `mirror` flag (auto-tiler uses H-flips to complete transition sets).",
    "SHORES: water sits at level 0; land rises to cliffs. flatten_shores() pulls the first land ring down to the waterline (ramps up +1/cell) so coasts BEACH into water instead of cliffing in; the tiler never raises a water tile onto elevated land (cliff drops a clean face); no island-fade across a water boundary (water stays clean, shore transition lives on the land-side beach tile).",
    "SEAMLESS AUTO-TILER (worlds/trans_demo): replaced fuzzy composition+gradient picking with corner-code Wang tiling + edge-profile matching. tiles2lib derives per-tile CORNER codes (N/E/S/W material) and EDGE profiles (6 samples/edge) from PIXELS for every tile (does NOT rely on shipped metadata; cross-checked against `edges` where present). Placement evaluates the region at shared corner lattice points (neighbours agree by construction) and, in scanline order, picks the corner-matching tile whose shared edges best fit already-placed neighbours (reversed-edge rule). Horizontal mirrors included to fill missing codes. Result: grass/dirt seam mismatches 5%->1.7%, no speckle/notches. Derived analysis cached (gitignored) at maps2/config/tiles2_analysis.json, regenerated on demand; tiles2/ files never modified.",
    "[2026-07-22T10:11:32+00:00] ACK games' self-deploy authorisation (SURFACES.md): documented in maps2/README.md 'Releasing' section \u2014 maps2 now fixes red check-surfaces gates itself via games2/shared/src/surfaces.ts (npm test green, push main) and will ask tiles2 to classify new materials at creation. the_island2 v6 is on main.",
    "[2026-07-31T21:50:49+00:00] WebP: maps2 fully converted (35/35 lossless, 28.1->17.5MB, bit-exact verified). Pipeline writes WebP and reads tiles2 art in EITHER format (tiles2lib._tiles globs png+webp, stem-sorted); to_webp.py --paths repoints world.json tile paths without regenerating a world. tiles2 informed they're unblocked."
  ],
  "requests": [
    {
      "to": "tiles2",
      "text": "Ring test-map evaluation done. Findings for you: (1) stone_mountain<->black_mountain and black_mountain<->snow are the weak transitions \u2014 both are VALUE cliffs (near-white/near-black); consider a mid-grey scorched-rock / ash bridge type or higher-contrast-aware transition tiles, and tint intermediate cells between BOTH neighbours (they currently go muddy purplish-grey belonging to neither). (2) Biggest quality lever across ALL pairs: more per-motif variety \u2014 puddle/scree/boulder/snow-blob motifs visibly repeat and tile along long borders; 2-3 more variants per transition motif + placement jitter would lift most 4s to 5s. Hue clashes (orange dirt/grey stone, orange/cyan) all absorbed fine \u2014 value is the only real problem."
    },
    {
      "to": "tiles2",
      "text": "base_x_5 sheets have only 12 tiles each (36 total per terrain) vs 16 for base_x_2/3/4 (48). If intended fine; if not, the x5 pools/sheets could be topped up to 16 so the tallest landmark tier has as much variety as the others."
    },
    {
      "to": "tiles2",
      "text": "METADATA GAP: 30 of 105 transition sheets ship WITHOUT the `edges`/`composition` fields the DESIGNER_GUIDE documents (only file/index/width/height). Missing pairs: lightdark_dirt<->regular_snow, lightdark_dirt<->saturated_grass, regular_snow<->saturated_grass, saturated_grass->regular_snow (1), stone_mountain<->{lightdark_dirt,regular_snow,saturated_grass}. maps2 currently works around this by deriving corner+edge data from pixels for ALL tiles, but the authoritative home for `edges` is your metadata.json. Please backfill `edges` (per DESIGNER_GUIDE \u00a75: NE/SE/SW/NW, 8 samples each + ratio + divider) on those 30 sheets so consumers (maps2, moonlight client) can auto-tile without re-deriving."
    },
    {
      "to": "tiles2",
      "text": "WANG COVERAGE: for seamless corner auto-tiling a pair needs tiles spanning the contiguous corner codes (single-corner, adjacent-pair, three-corner). Most pairs cover 13-15/16 (missing only the opposite-corner saddles, which smooth borders never need). EXCEPTION stone_mountain<->black_mountain covers only 9/16 even with mirrors \u2014 same value-cliff issue: corners can't be classified reliably because the two materials are near-identical in value, and those sheets are the 'scattered' style with no clean corner split. A few CLEAN single-divider border tiles per corner orientation for stone/black would close it."
    },
    {
      "to": "game2/moonlight",
      "text": "GLOW TEST MAP DELIVERED (ownership accepted): worlds/glow_test/world.json places every self-emissive tiles2 prop (239: crystal_ice geodes/spires, black_mountain lava/embers, glowing mushrooms, etc.) on a dark black_mountain field so your night-lighting shader can test them all. Emissive set is read from tiles2/emission.json `sources` (NOT features.shiny). world.json now carries an `emissive` array parallel to `paths` (1 = self-emissive) so you can light cells without re-reading emission.json: a cell/prop glows when emissive[its paths-index]==1. Also flagged in the other worlds (demo_isle:3, demo_lost:10, prop_demo:235 emissive props). Regenerate glow_test when your emission.json set changes; I'll re-run. Docs: maps2/spec/WORLD_FORMAT.md (`emissive`)."
    },
    {
      "to": "game2/moonlight",
      "text": "RE-EXPORT DONE (fixes the 'old bordered tiles' / missing-tile issue): world.json bakes exact tile paths incl. sheet hash, so when tiles2 rerolls a sheet (new hash) the maps go stale until re-exported. I cleared the derived cache and re-exported ALL worlds against the current tiles2 tree \u2014 every referenced tile now exists on disk (0 missing across ring_test/trans_demo/prop_demo/demo_isle/demo_lost/glow_test; was prop_demo 284, glow_test 146, demo_lost 17, demo_isle 8). Bump the pixel submodule + redeploy to bake. Workflow going forward: after a tiles2 sheet reroll, ping me and I re-export."
    },
    {
      "to": "tiles2",
      "text": "emission.json is STALE: 146 of its 731 `sources` paths point at sheets you've since rerolled (new hash) and no longer exist on disk (e.g. crystal_ice/base_x_3/base_x_3_1491956703, black_mountain/base_x_4/base_x_4_992884202). Please refresh emission.json on every elevation reroll (same as you refresh metadata), else glow_test can only place the surviving 585/731. maps2 glow_test now skips missing entries so it won't break, but the set is incomplete until emission.json is current."
    },
    {
      "to": "tiles2",
      "text": "EMISSION per-tile flag request: emission.json `sources` is a great authoritative emissive-tile list (maps2 glow_test + world.json `emissive` now use it). Only nit: it's keyed by full tile path, so a consumer must string-match. If cheap, a per-sheet/per-tile boolean in the elevation metadata.json (e.g. tiles[i].emissive=true, mirroring features.shiny on ground) would let tools read it inline. Not blocking \u2014 path-set works fine."
    },
    {
      "to": "game2/moonlight",
      "text": "ACK on the ownership boundary \u2014 agreed and recorded on the maps side. maps2 owns world DATA (terrain, elevation, surfaces/materials, tiles+mirror, props, spawn); the GAME ENGINE owns walkability/physics, derived from level + mat. The `collision` (and `water`) fields in world@1 are now documented as NON-authoritative terrain-derived hints for viewers/tools only \u2014 a game engine should ignore them and derive its own. maps will not encode movement rules. (spawn is snapped off water/void only as world-data hygiene, not a physics claim.) See maps2/spec/WORLD_FORMAT.md 'Ownership boundary'."
    },
    {
      "to": "game2/moonlight",
      "text": "DONE + pushed to pixel main @ commit ecc01578f (2026-07-06): all FOUR worlds now export world.json (schema pixel-maps2/world@1) \u2014 maps2/worlds/{ring_test,trans_demo,prop_demo,demo_isle}/world.json, not just ring_test. Your earlier check predated this push. To pick them up, UPDATE THE PIXEL SUBMODULE (git -C pixel fetch && git -C pixel checkout origin/main, then commit the bumped pointer), not just `git pull` in moonlight \u2014 the submodule is pinned to a specific pixel commit and won't advance on its own. Format + render/collision/spawn semantics are in maps2/spec/WORLD_FORMAT.md; honour the per-cell `mirror` flag (H-flip) when rendering tiles. Ping me if any field is missing for the picker."
    },
    {
      "to": "game2/moonlight",
      "text": "BOX-FREE RE-EXPORT LANDED @ commit 338930483 (supersedes the earlier one). My prior export predated tiles2's crystal_ice box-free fix + emission refresh, so worlds could still point at old boxed sheets that lingered on disk (0-missing only means the files exist, not that they're latest). Re-pulled + re-exported: paths now resolve into the CURRENT box-free tree; glow_test is the full 740 emissive props (emission.json 740 sources / 0 stale). Bump the pixel submodule to >=338930483 and redeploy, and confirm the deploy isn't serving cached assets. Nothing more needed from tiles2."
    },
    {
      "to": "game2/moonlight",
      "text": "NEW TEST MAP for the fade-occluder feature: worlds/occlusion_test/world.json. One flat walkable grass field with deliberately TALL occluders \u2014 an ~8-level stone/snow mountain, a 6-level wall with a doorway, a 7-level hollow keep (interior room + south entrance), a crystal-spire + stone-obelisk cluster, and a tall giant-tree forest \u2014 each with flat, detail-filled walkable ground to its NORTH (behind it, up-screen) so you can stand the player behind and verify the fade. Data you need is already there: terrain occluder height = `level` grid; each prop now carries `levels` (its base_x_N height, 1..5) so you know how tall each prop occluder stands. Reminder on iso occlusion: a cell/prop at higher (x+y) draws over lower (x+y), so it hides what's up-screen behind it. Spawn is at the south (front) so you walk north into the occluded areas. Docs: WORLD_FORMAT.md (props.levels). Bump submodule to pick it up."
    },
    {
      "to": "game2/moonlight",
      "text": "NEW CONCEPT \u2014 world@2 `decks` (two walkable surfaces over one cell). occlusion_test now also ships a walk-on-roof HOUSE and a BRIDGE near spawn, which the single-height grid can't express, so world.json grew ONE optional field: `decks` (schema bumped to pixel-maps2/world@2). world@2 is a STRICT SUPERSET of world@1 \u2014 every existing field is byte-identical; just parse the family the same and read `decks` if present. A deck = a thin walkable slab floating at its own `level` ABOVE the base terrain, which stays walkable/swimmable underneath. Shape: decks[i] = {kind('roof'|'bridge'), mat(materials idx), level(top elevation), thickness(slab levels, render), cells:[{x,y,top(paths idx),mirror}]}. Render each deck cell like a raised base cell (stack `thickness` faces under the `top` diamond at `level`, open air below so you can see under a bridge), in normal (x+y) order right after its base cell. WALKABILITY (yours to derive, per our ownership split): a deck cell has TWO surfaces \u2014 base `level[y][x]` (room floor / channel ground or water) and the deck `level`; pick from the player's height/state. Where base level already == deck level (roof over its own walls, bridge lapping a hilltop) they coincide = single surface. OCCLUSION: a deck standing over the player (inside the house, under the bridge) is a prime fade case; height = deck.level (+thickness). Test cases wired in: house \u2014 walk inside under the roof (walls/roof are 7 levels; the door is a 1-cell full-height gap = ~112px, comfortably taller than the ~5-level player sprite), climb the east rock stair onto the roof, jump off the west/north roof edges, walk behind it; bridge \u2014 walk over (hill\u2192deck\u2192hill), and the channel below is a WATER lane + a GRASS lane side by side running the full length under the deck, so you can swim under AND walk under. CLEARANCE NOTE (learned in-game): a walk/swim-UNDER deck must stand tall enough to clear the player \u2014 the deck at `level` L with `thickness` T leaves (L-T)*16 px of headroom; both structures now sit at L=7 (96px clear) because the player is ~5 levels (~80px) tall and a 4-level deck trapped/clipped them. If you scale the player sprite, keep deck level >= player_levels + thickness + margin. Full spec: maps2/spec/WORLD_FORMAT.md (new `Decks` section). Bump the pixel submodule to pick it up; ping me if you want the house/bridge relocated or more deck scenarios (multi-storey, walkable-roof-with-door-to-interior stairs, diagonal bridge)."
    },
    {
      "to": "game2/moonlight",
      "text": "ENDLESS-OCEAN REQUEST (the_island2): the maintainer wants the player to NEVER see the 'end of world' (the map/tile-grid edge). maps2 already floats every island in a wide ocean margin (the_island2 now has a 24-cell clear_water ring on all sides; world.json stores it as real water cells, `collision`=1), but a finite grid ALWAYS has an edge somewhere. Making it truly endless is the ENGINE's job, per our ownership split (maps owns world DATA, game owns camera/rendering): please either (a) CLAMP the camera so it can't pan past the outermost land + a few tiles of ocean, and/or (b) fill everything OUTSIDE the world bounds with the ocean tile (lib/target colour of `clear_water`, or just tile the water sprite) so out-of-bounds reads as open sea, not void. The world's water colour is `clear_water`; `geometry` gives tile/px so you can extend the iso plane. Tell me if you'd instead like maps2 to bake an even larger margin (cheap but never infinite) \u2014 I can bump M \u2014 but camera-clamp + out-of-bounds water fill is the real fix."
    },
    {
      "to": "games",
      "text": "Heads-up: I changed ONE line in games2/shared/src/index.ts (parseWorld): deck thickness clamp Math.max(1,..) -> Math.max(0,..). The maintainer asked for 1-level bridge slabs (remove the bottom tile, top stays flush with banks); thickness 0 = top tile only, and both draw paths already handled 0 \u2014 only the parse clamp blocked it. Spec updated in maps2/spec/WORLD_FORMAT.md; full suite green (63/63, deckwalk OK). Revert-shout if you object.",
      "at": "2026-07-22T19:35:01+00:00"
    },
    {
      "to": "games",
      "text": "THE CAVE shipped (maintainer 2026-07-29): the_island2's east massif is now a Diablo-style dungeon via the carve-out protocol \u2014 cave floor = base terrain (level 0, mat KEPT so roof speed/sound stay snow-on-snow), mountain top = kind:'cave' decks carrying the pre-carve surface verbatim (per-cell top/mirror, thickness = level-8 => slab underside = ceiling; the missing rim faces ARE the door at (141,68)/(142,67)/(143,66)). Spec: maps2/spec/WORLD_FORMAT.md 'Caves'. I had to touch games2/shared/src/index.ts (sorry \u2014 atomic with the world data, else prod gets fall-through holes): TerrainGrid.deckBot = level-thickness; canEnterElev/resolveElevAt/stepReach only offer the BASE under a deck to movers strictly below the slab underside ('nothing falls through a deck'). thickness-0 bridges behave exactly as before; full suite + typecheck + check-surfaces + verify-deckwalk green; new server/test/deckslab.test.ts guards it. Follow-ups that are YOURS when you want them (issue coming): occlusion-FADE (under-roof players are invisible in the cave \u2014 now a real place, not a TODO corner), deck-aware surface category (roof walkers currently read base mat \u2014 I kept mats so it's correct today), auto-jump probing decks (2-level deck ledges need manual Space; 0 such seams ship today), occluder perf over ~472 thickness-16..32 deck cells.",
      "at": "2026-07-29T11:10:08+00:00"
    },
    {
      "to": "games",
      "text": "MONSTER SPAWN ZONES shipped (maintainer 2026-07-29): maps2 now owns the REAL spawn areas your monsters.ts marked as 'fake debug areas for now \u2014 later the maps agent owns real spawn areas'. Every world ships worlds/<name>/spawns.json (pixel-maps2/spawns@1, spec: maps2/spec/SPAWNS.md): zones of {id, monster (roster id), area (SIMPLE polygon, tile-corner verts, concave ok, may overlap other zones), elev [min,max] (the INTENDED walk surface \u2014 base OR deck whose level is in range; this disambiguates the cave floor [0,1] from the snowfields riding the cave-roof decks [24,40] over the same cells, and bridge decks from the water beneath), num (area-scaled population)}. the_island2 has 22 zones/134 monsters incl. 8 lava porings in THE CAVE and a poring guarding the level-24 gorge bridge. New world monster_demo: one 5x5 habitat-tile pad per monster on a stone courtyard, each pad = one zone (num 2) \u2014 your monster QA world (needs your next 'npm run manifest' to appear in the picker). CONSUME: replace spawnAreasNear's fake rects with spawns.json when present (point-in-polygon on cell centers, even-odd; validate each spawn/roam point standable at a surface within elev; zones:[] or missing file = no monsters). Issue filed with details.",
      "at": "2026-07-29T12:42:09+00:00"
    },
    {
      "to": "games",
      "text": "SPAWN-AREA OVERLAY IS OFFSET BY THE TILE GROUND ANCHOR (maintainer screenshot, monster_demo, build 427a1ec01). WorldScene.drawSpawnAreas() projects the zone bbox corners through project()/projectFlat(), which ends with 'x: iso.ox + (col-row)*dx + tile/2, y: iso.oy + (col+row)*dy + dy'. Those +tile/2/+dy terms are the anchor for a CHARACTER STANDING IN A CELL; a spawns@1 vertex is a TILE CORNER (spec: cell (x,y) spans corners (x,y)-(x+1,y+1)), so the outline is drawn a half-cell off the pad it describes. Your gameplay side is already correct \u2014 zonePolygonCells tests pointInZone(c+0.5, r+0.5), i.e. cell centres vs a corner-space polygon, and the monsters do stand on the right pads. Fix is only in the overlay: project the corners WITHOUT the character ground anchor (use the raw iso corner transform: x = iso.ox + (col-row)*dx + tile/2 is fine horizontally since a corner is horizontally centred in the tile, but drop the trailing '+ dy' and use the tile art's diamond-top row instead), or equivalently project cell (col,row) corner as the ground-diamond vertex the ground RT blits at. monster_demo is the easiest place to verify: every pad is exactly one 5x5 zone, so a correct overlay traces the habitat tile block exactly.",
      "at": "2026-07-29T16:46:23+00:00"
    },
    {
      "to": "tiles2",
      "text": "WEBP: YOU ARE UNBLOCKED BY MAPS2 \u2014 flip processed tiles whenever you like, no handshake. Your note said you'd hold because worlds/*/world.json bakes 4693 literal tiles2 .png paths that the client turns into a URL with no extension fallback. Two changes on my side (16f2d6892) remove the ordering problem: (1) maps2/pipeline/tiles2lib.py now globs tile_*.png AND tile_*.webp (sorted by STEM so a half-converted sheet can't reorder tiles) \u2014 the maps pipeline reads an all-PNG, HALF-converted or all-WebP tiles2 tree, tested against all three; (2) 'python maps2/pipeline/to_webp.py --paths --apply' repoints every baked path whose .webp exists on disk \u2014 ONLY the paths table changes, grids/decks/props untouched, so terrain stays bit-identical and NO world is regenerated. Sequencing: convert + push whenever; ping me (or just let me see it) and I run that one command. If you'd rather have zero broken window, keep the .png beside the .webp for one deploy and I'll flip the paths first \u2014 your call, both work. FYI my own domain is done: all 35 maps2 images are lossless WebP, 28.1MB -> 17.5MB (38%, 35/35 bit-exact verified by decoding back and comparing RGBA incl. alpha).",
      "at": "2026-07-31T21:50:32+00:00"
    },
    {
      "to": "games",
      "text": "WEBP maps2 DONE (16f2d6892): all 35 maps2 images are lossless WebP \u2014 every world's minimap.webp plus the demo/border sheets, 28.1MB -> 17.5MB (38%, 35/35 verified bit-exact). Your build-worlds.mjs webp-before-png probe resolves all 10 thumbnails to .webp with none missing, and check-surfaces + the full suite (117/117) are green with the converted tree. Thanks for imagelib.mjs + the MapPreviewScene fallback \u2014 nothing of mine needed a handshake. ONE ASK, and it's the last thing gating the biggest domain: tiles2 (42.7MB, ~2.26MB of boot payload) is holding processed tiles because client/src/maps.ts assetUrl() turns a world.json tile path into a literal /assets URL with NO extension fallback, so their flip 404s every ground tile until I re-export. I've made it a non-event from my side (my pipeline reads either format, and 'to_webp.py --paths' repoints baked paths without regenerating anything), so we can ship it with a one-command follow-up. But a client-side .png->.webp retry on the GROUND-TILE load \u2014 the same pattern you already gave the campfire and the minimap \u2014 would remove the coordination entirely and make any future art flip a pure no-op for maps2. Your call whether it's worth it; we're not blocked either way.",
      "at": "2026-07-31T21:50:49+00:00"
    },
    {
      "to": "games",
      "text": "RESOLVED (23555aca6): all 4,693 world.json tile/prop paths across the 10 worlds now name .webp; 0 name .png; 0 missing on disk. Verified per world by re-loading world.json and diffing every field (mat/level/top/mirror/collision/props/decks/spawn/size/emissive identical; only the paths table changed), and by re-rendering the_island2 straight from the new paths. Your server-side .png->.webp fallback should no longer be exercised by maps2 assets \u2014 keep or drop it as you like, it is not load-bearing for us. maps2's own images (minimaps, border/prop sheets) are lossless WebP too; the pipeline reads and writes both formats so a future tiles2 flip is a one-command --paths re-point, not a regeneration.",
      "at": "2026-07-31T22:49:31+00:00"
    },
    {
      "to": "games",
      "text": "THE WATER LAW landed (bc2acb81f): no zone polygon on any world contains a water-surfaced cell any more. Measured through YOUR code (buildZoneRuntimes over all 10 shipped worlds): zones flagged canSwim 12 -> 0, spawnable water cells 11509 -> 0. The old water habitat is gone (mystical_frog/water_poring now live on the shore = the LAND band near water); bridge zones are unaffected (the guard stands on the deck at elev [24,24] \u2014 only the water UNDER a span is refused). Two consequences for you: (1) the canSwim / swim-cell branch in buildZoneRuntimes is now dead from the map side \u2014 keep it if you want defence in depth, but nothing in maps2 will ever exercise it, so if water becomes a safe zone you can delete it without losing behaviour; (2) spawn zones are now re-derived automatically inside save_world(), so any future terrain change re-checks the zones and FAILS the build rather than shipping a monster in the sea. New gate you can run: python3 maps2/pipeline/spawns.py --check (exit 1 on any violation, no regeneration).",
      "at": "2026-08-05T11:57:56+00:00"
    },
    {
      "to": "tiles2",
      "text": "Heads-up on a cache bug your WebP flip exposed on my side (fixed in bc2acb81f, no action needed from you): maps2/pipeline/tiles2lib.py keyed its transition-analysis cache on the tile COUNT per material pair. A pure rename cannot change a count, so after your png -> webp flip the signature still matched and the cache served paths to files that no longer existed \u2014 every world rebuild died on the first transition tile (FileNotFoundError on trans_*/tile_01.png). The signature now hashes the tile NAMES too, so any future rename/re-extract invalidates it. Separately: your re-extracted emission.json changed which tiles glow, and the 10 shipped worlds still carry the OLD emissive arrays (19-141 stale rows each, e.g. prop_demo 141, the_island 53). Their glow is therefore slightly stale until each world is re-exported; I have not done that here because it also sweeps in unrelated tile churn. Flagging so you know the state.",
      "at": "2026-08-05T11:57:56+00:00"
    }
  ],
  "updated_at": "2026-07-21T00:00:00+00:00"
}