{
  "_comment": "tiles2 — breaking change from tiles v1. Each ground type is a SPECIFICALLY named subfolder (saturated_grass, not 'grass') so more variants can be added later. Flow: generate -> raw/ (+ per-sheet metadata, kind=base|transition) -> postprocess copies into base/ or transitions/<other>/, normalizing brightness/hue/saturation to the ref-sprite(s). Raw is always kept so postprocess can be re-tuned and re-run.",
  "tile": {
    "type": "isometric",
    "size": 64,
    "view": "high top-down",
    "view_angle": 28.0,
    "depth_ratio": 0.5,
    "flat_top_px": 2,
    "outline": "none"
  },
  "prompt": {
    "_comment": "Prepended/appended to every prompt to push the no-outline, cohesive look (create-tiles-pro has no outline param, so we ask for it here and strip residue in postprocess).",
    "style": "clean lineless pixel art, NO black outline, no dark tile borders, seamless isometric terrain tile, soft matte shading, cohesive muted palette",
    "base_template": "isometric {description} terrain tiles, every tile is {name} only, {style}: {variations}",
    "transition_template": "isometric terrain transition tiles from {from_desc} to {to_desc}, {style}: {variations}",
    "base_variations": "1) even ground 2) subtle texture variation 3) faint detail 4) slightly darker patch 5) slightly lighter patch 6) minimal specks",
    "transition_variations": "1) mostly {from_name} with {to_name} entering from the north-east 2) mostly {from_name} with {to_name} entering from the north-west 3) even diagonal split {from_name} to {to_name} 4) mostly {to_name} with {from_name} entering from the south-east 5) a patch of {to_name} within {from_name} 6) a soft ragged border between {from_name} and {to_name}"
  },
  "ground_types": [
    { "id": "saturated_grass", "name": "saturated grass", "description": "lush saturated deep green grass" },
    { "id": "regular_snow",    "name": "snow",            "description": "clean fresh white snow" },
    { "id": "normal_dirt",     "name": "dirt",            "description": "plain packed brown dirt / bare earth" },
    { "id": "stone_mountain",  "name": "grey mountain stone", "description": "rugged grey mountain rock and stone" },
    { "id": "black_mountain",  "name": "black volcanic rock", "description": "dark near-black volcanic mountain rock" },
    { "id": "clear_water",     "name": "clear water",     "description": "clear calm blue water" }
  ],
  "transitions": [
    ["saturated_grass", "normal_dirt"],
    ["saturated_grass", "clear_water"],
    ["saturated_grass", "regular_snow"],
    ["normal_dirt", "stone_mountain"],
    ["stone_mountain", "black_mountain"],
    ["stone_mountain", "regular_snow"],
    ["normal_dirt", "clear_water"],
    ["regular_snow", "clear_water"]
  ],
  "targets": {
    "_comment": "How many raw BASE tile-sheets to accumulate per ground type before the loop moves on to transitions. Each sheet ~16 tiles / ~20 generations.",
    "base_sheets_per_type": 3,
    "transition_sheets_per_pair": 1
  },
  "budget": {
    "min_generations_remaining": 500,
    "_comment": "Shared PixelLab pool (coordination/PROTOCOL.md). Keep a floor for the other agents."
  },
  "postprocess": {
    "_comment": "raw -> base/transitions. neutralize_outline removes any residual dark rim (tiles2 = no outline). strength (0..1) scales colour-normalisation toward the ref-sprite(s).",
    "neutralize_outline": true,
    "darkness_thresh": 60,
    "strength": 1.0
  }
}
