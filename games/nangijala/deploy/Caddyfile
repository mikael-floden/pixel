# Auto-HTTPS reverse proxy for Nangijala. Caddy fetches a Let's Encrypt cert for
# the domain and transparently proxies WebSockets to the game container.
nangijala.online {
	reverse_proxy localhost:8080
}

# Optional: redirect www → apex
www.nangijala.online {
	redir https://nangijala.online{uri}
}
